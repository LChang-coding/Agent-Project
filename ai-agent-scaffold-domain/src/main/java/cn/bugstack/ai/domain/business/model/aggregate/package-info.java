/**
 * 业务域「聚合根」的预留包：聚合根是一组必须一起改、一起校验的数据的总入口，
 * 外部只能通过聚合根修改内部数据，从而保证一次操作后整组数据仍然是合法的。
 *
 * <p>所属层次：领域层（domain）的模型部分，不含任何框架代码。</p>
 *
 * <p>谁会用它：业务域的领域服务负责取出聚合根、调用它的方法完成状态变更，再交给仓储端口保存。
 * 触发器层和应用层不应该直接操作聚合根内部字段。</p>
 *
 * <p>约束：聚合根只维护聚合内部的不变量（例如「运行结束时间不能早于开始时间」），
 * 不允许在这里注入或调用仓储、HTTP 客户端、消息队列等外部能力，否则领域模型就无法脱离基础设施单独测试。</p>
 *
 * <p>当前状态：脚手架预留的空包。实际在跑的聚合示例可参考 {@code rag}、{@code workflow} 等子域的模型包。</p>
 */
package cn.bugstack.ai.domain.business.model.aggregate;
