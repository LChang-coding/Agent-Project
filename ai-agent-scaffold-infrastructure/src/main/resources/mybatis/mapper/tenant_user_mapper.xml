<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.ITenantUserDao">

    <resultMap id="TenantUserMap" type="cn.bugstack.ai.infrastructure.dao.po.TenantUserPO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="user_id" property="userId"/>
        <result column="role_code" property="roleCode"/>
        <result column="status" property="status"/>
        <result column="joined_time" property="joinedTime"/>
        <result column="metadata" property="metadata"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="TenantUserColumns">
        id, tenant_id, user_id, role_code, status, joined_time, metadata, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.TenantUserPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO tenant_user (tenant_id, user_id, role_code, status, joined_time, metadata)
        VALUES (#{tenantId}, #{userId}, #{roleCode}, #{status}, #{joinedTime}, #{metadata})
    </insert>

    <update id="updateById" parameterType="cn.bugstack.ai.infrastructure.dao.po.TenantUserPO">
        UPDATE tenant_user
        SET tenant_id = #{tenantId}, user_id = #{userId}, role_code = #{roleCode}, status = #{status}, joined_time = #{joinedTime}, metadata = #{metadata}
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="queryById" resultMap="TenantUserMap">
        SELECT <include refid="TenantUserColumns"/>
        FROM tenant_user
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="queryListByTenantId" resultMap="TenantUserMap">
        SELECT <include refid="TenantUserColumns"/>
        FROM tenant_user
        WHERE tenant_id = #{tenantId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByUserId" resultMap="TenantUserMap">
        SELECT <include refid="TenantUserColumns"/>
        FROM tenant_user
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY id DESC
    </select>

</mapper>
