<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.IMcpServerConfigDao">

    <resultMap id="McpServerConfigMap" type="cn.bugstack.ai.infrastructure.dao.po.McpServerConfigPO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="visibility" property="visibility"/>
        <result column="mcp_id" property="mcpId"/>
        <result column="mcp_name" property="mcpName"/>
        <result column="transport_type" property="transportType"/>
        <result column="endpoint" property="endpoint"/>
        <result column="command" property="command"/>
        <result column="args" property="args"/>
        <result column="env" property="env"/>
        <result column="description" property="description"/>
        <result column="current_version" property="currentVersion"/>
        <result column="published_version" property="publishedVersion"/>
        <result column="active_version_id" property="activeVersionId"/>
        <result column="test_status" property="testStatus"/>
        <result column="test_message" property="testMessage"/>
        <result column="last_test_time" property="lastTestTime"/>
        <result column="status" property="status"/>
        <result column="metadata" property="metadata"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="McpServerConfigColumns">
        id, tenant_id, owner_user_id, visibility, mcp_id, mcp_name, transport_type, endpoint, command, args, env, description, current_version, published_version, active_version_id, test_status, test_message, last_test_time, status, metadata, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.McpServerConfigPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO mcp_server_config (tenant_id, owner_user_id, visibility, mcp_id, mcp_name, transport_type, endpoint, command, args, env, description, current_version, published_version, active_version_id, test_status, test_message, last_test_time, status, metadata)
        VALUES (#{tenantId}, #{ownerUserId}, #{visibility}, #{mcpId}, #{mcpName}, #{transportType}, #{endpoint}, #{command}, #{args}, #{env}, #{description}, COALESCE(#{currentVersion}, '1.0.0'), #{publishedVersion}, #{activeVersionId}, COALESCE(#{testStatus}, 'untested'), #{testMessage}, #{lastTestTime}, COALESCE(#{status}, 'draft'), #{metadata})
    </insert>

    <update id="updateById" parameterType="cn.bugstack.ai.infrastructure.dao.po.McpServerConfigPO">
        UPDATE mcp_server_config
        SET tenant_id = #{tenantId}, owner_user_id = #{ownerUserId}, visibility = #{visibility}, mcp_id = #{mcpId}, mcp_name = #{mcpName}, transport_type = #{transportType}, endpoint = #{endpoint}, command = #{command}, args = #{args}, env = #{env}, description = #{description}, current_version = #{currentVersion}, published_version = #{publishedVersion}, active_version_id = #{activeVersionId}, test_status = #{testStatus}, test_message = #{testMessage}, last_test_time = #{lastTestTime}, status = #{status}, metadata = #{metadata}
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="queryById" resultMap="McpServerConfigMap">
        SELECT <include refid="McpServerConfigColumns"/>
        FROM mcp_server_config
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="queryByMcpId" resultMap="McpServerConfigMap">
        SELECT <include refid="McpServerConfigColumns"/>
        FROM mcp_server_config
        WHERE mcp_id = #{mcpId} AND deleted = 0
    </select>

    <select id="queryListByTenantId" resultMap="McpServerConfigMap">
        SELECT <include refid="McpServerConfigColumns"/>
        FROM mcp_server_config
        WHERE tenant_id = #{tenantId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByOwnerUserId" resultMap="McpServerConfigMap">
        SELECT <include refid="McpServerConfigColumns"/>
        FROM mcp_server_config
        WHERE owner_user_id = #{ownerUserId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByTenantIdAndVisibility" resultMap="McpServerConfigMap">
        SELECT <include refid="McpServerConfigColumns"/>
        FROM mcp_server_config
        WHERE tenant_id = #{tenantId} AND visibility = #{visibility} AND deleted = 0
        ORDER BY id DESC
    </select>

</mapper>
