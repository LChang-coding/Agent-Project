<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.IRagKnowledgeBaseDao">

    <resultMap id="RagKnowledgeBaseMap" type="cn.bugstack.ai.infrastructure.dao.po.RagKnowledgeBasePO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="visibility" property="visibility"/>
        <result column="kb_id" property="knowledgeBaseId"/>
        <result column="kb_name" property="knowledgeBaseName"/>
        <result column="description" property="description"/>
        <result column="embedding_model" property="embeddingModel"/>
        <result column="status" property="status"/>
        <result column="metadata" property="metadata"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="RagKnowledgeBaseColumns">
        id, tenant_id, owner_user_id, visibility, kb_id, kb_name, description, embedding_model, status, metadata, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.RagKnowledgeBasePO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rag_knowledge_base (tenant_id, owner_user_id, visibility, kb_id, kb_name, description, embedding_model, status, metadata)
        VALUES (#{tenantId}, #{ownerUserId}, #{visibility}, #{knowledgeBaseId}, #{knowledgeBaseName}, #{description}, #{embeddingModel}, #{status}, #{metadata})
    </insert>

    <update id="updateById" parameterType="cn.bugstack.ai.infrastructure.dao.po.RagKnowledgeBasePO">
        UPDATE rag_knowledge_base
        SET tenant_id = #{tenantId}, owner_user_id = #{ownerUserId}, visibility = #{visibility}, kb_id = #{knowledgeBaseId}, kb_name = #{knowledgeBaseName}, description = #{description}, embedding_model = #{embeddingModel}, status = #{status}, metadata = #{metadata}
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="queryById" resultMap="RagKnowledgeBaseMap">
        SELECT <include refid="RagKnowledgeBaseColumns"/>
        FROM rag_knowledge_base
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="queryByKnowledgeBaseId" resultMap="RagKnowledgeBaseMap">
        SELECT <include refid="RagKnowledgeBaseColumns"/>
        FROM rag_knowledge_base
        WHERE kb_id = #{knowledgeBaseId} AND deleted = 0
    </select>

    <select id="queryListByTenantId" resultMap="RagKnowledgeBaseMap">
        SELECT <include refid="RagKnowledgeBaseColumns"/>
        FROM rag_knowledge_base
        WHERE tenant_id = #{tenantId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByOwnerUserId" resultMap="RagKnowledgeBaseMap">
        SELECT <include refid="RagKnowledgeBaseColumns"/>
        FROM rag_knowledge_base
        WHERE owner_user_id = #{ownerUserId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByKnowledgeBaseId" resultMap="RagKnowledgeBaseMap">
        SELECT <include refid="RagKnowledgeBaseColumns"/>
        FROM rag_knowledge_base
        WHERE kb_id = #{knowledgeBaseId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByTenantIdAndVisibility" resultMap="RagKnowledgeBaseMap">
        SELECT <include refid="RagKnowledgeBaseColumns"/>
        FROM rag_knowledge_base
        WHERE tenant_id = #{tenantId} AND visibility = #{visibility} AND deleted = 0
        ORDER BY id DESC
    </select>

</mapper>
