<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.IRagDocumentDao">

    <!-- 文档聚合按租户和 knowledgeBaseId 隔离；活动版本切换必须在锁内校验 revision。 -->
    <resultMap id="RagDocumentMap" type="cn.bugstack.ai.infrastructure.dao.po.RagDocumentPO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="visibility" property="visibility"/>
        <result column="kb_id" property="knowledgeBaseId"/>
        <result column="document_id" property="documentId"/>
        <result column="asset_id" property="assetId"/>
        <result column="file_name" property="fileName"/>
        <result column="source_type" property="sourceType"/>
        <result column="source_uri" property="sourceUri"/>
        <result column="source_bucket" property="sourceBucket"/>
        <result column="source_object_key" property="sourceObjectKey"/>
        <result column="mime_type" property="mimeType"/>
        <result column="size_bytes" property="sizeBytes"/>
        <result column="content_hash" property="contentHash"/>
        <result column="document_version" property="documentVersion"/>
        <result column="active_generation" property="activeGeneration"/>
        <result column="active_version_id" property="activeVersionId"/>
        <result column="target_generation" property="targetGeneration"/>
        <result column="parser_name" property="parserName"/>
        <result column="parser_version" property="parserVersion"/>
        <result column="page_count" property="pageCount"/>
        <result column="chunk_count" property="chunkCount"/>
        <result column="last_error_code" property="lastErrorCode"/>
        <result column="last_error_message" property="lastErrorMessage"/>
        <result column="indexed_at" property="indexedAt"/>
        <result column="revision" property="revision"/>
        <result column="status" property="status"/>
        <result column="metadata" property="metadata"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="Columns">
        id, tenant_id, owner_user_id, visibility, kb_id, document_id, asset_id, file_name,
        source_type, source_uri, source_bucket, source_object_key, mime_type, size_bytes, content_hash,
        document_version, active_generation, active_version_id, target_generation, parser_name,
        parser_version, page_count, chunk_count, last_error_code, last_error_message, indexed_at,
        revision, status, metadata, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.RagDocumentPO"
            useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rag_document (
            tenant_id, owner_user_id, visibility, kb_id, document_id, asset_id, file_name,
            source_type, source_uri, source_bucket, source_object_key, mime_type, size_bytes, content_hash,
            document_version, active_generation, active_version_id, target_generation, parser_name,
            parser_version, page_count, chunk_count, last_error_code, last_error_message, indexed_at,
            revision, status, metadata
        ) VALUES (
            #{tenantId}, #{ownerUserId}, #{visibility}, #{knowledgeBaseId}, #{documentId}, #{assetId},
            #{fileName}, #{sourceType}, #{sourceUri}, #{sourceBucket}, #{sourceObjectKey}, #{mimeType},
            #{sizeBytes}, #{contentHash}, COALESCE(#{documentVersion}, 1), COALESCE(#{activeGeneration}, 0),
            #{activeVersionId}, #{targetGeneration}, #{parserName}, #{parserVersion}, #{pageCount},
            COALESCE(#{chunkCount}, 0),
            #{lastErrorCode}, #{lastErrorMessage}, #{indexedAt}, #{revision}, #{status}, #{metadata}
        )
    </insert>

    <select id="queryByTenantAndDocumentId" resultMap="RagDocumentMap">
        SELECT <include refid="Columns"/> FROM rag_document
        WHERE tenant_id = #{tenantId} AND document_id = #{documentId} AND deleted = 0 LIMIT 1
    </select>

    <select id="queryByTenantKnowledgeBaseAndDocumentIdForUpdate" resultMap="RagDocumentMap">
        SELECT <include refid="Columns"/> FROM rag_document
        WHERE tenant_id = #{tenantId} AND kb_id = #{knowledgeBaseId}
          AND document_id = #{documentId} AND deleted = 0
        LIMIT 1 FOR UPDATE
    </select>

    <select id="queryListByTenantAndDocumentIds" resultMap="RagDocumentMap">
        SELECT <include refid="Columns"/> FROM rag_document
        WHERE tenant_id = #{tenantId} AND document_id IN
        <foreach collection="documentIds" item="documentId" open="(" separator="," close=")">
            #{documentId}
        </foreach>
        AND deleted = 0
    </select>

    <select id="queryListByTenantAndKnowledgeBaseId" resultMap="RagDocumentMap">
        SELECT <include refid="Columns"/> FROM rag_document
        WHERE tenant_id = #{tenantId} AND kb_id = #{knowledgeBaseId} AND deleted = 0 ORDER BY id DESC
    </select>

    <select id="countByTenantAndKnowledgeBaseId" resultType="int">
        SELECT COUNT(*) FROM rag_document
        WHERE tenant_id = #{tenantId} AND kb_id = #{knowledgeBaseId} AND deleted = 0
    </select>

    <select id="countNotDeletedByTenantAndKnowledgeBaseId" resultType="int">
        SELECT COUNT(*) FROM rag_document
        WHERE tenant_id = #{tenantId} AND kb_id = #{knowledgeBaseId}
          AND status &lt;&gt; 'deleted' AND deleted = 0
    </select>

    <select id="queryListByTenantId" resultMap="RagDocumentMap">
        SELECT <include refid="Columns"/> FROM rag_document
        WHERE tenant_id = #{tenantId} AND deleted = 0 ORDER BY id DESC
    </select>

    <select id="queryListByTenantIdAndVisibility" resultMap="RagDocumentMap">
        SELECT <include refid="Columns"/> FROM rag_document
        WHERE tenant_id = #{tenantId} AND visibility = #{visibility} AND deleted = 0 ORDER BY id DESC
    </select>

    <update id="updateByTenantAndRevision">
        UPDATE rag_document
        SET owner_user_id = #{document.ownerUserId}, visibility = #{document.visibility},
            kb_id = #{document.knowledgeBaseId}, file_name = #{document.fileName},
            active_generation = #{document.activeGeneration},
            active_version_id = #{document.activeVersionId}, target_generation = #{document.targetGeneration},
            status = #{document.status}, revision = revision + 1
        WHERE tenant_id = #{tenantId} AND document_id = #{document.documentId}
          AND revision = #{expectedRevision} AND deleted = 0
    </update>

    <update id="activateVersionByTenantAndRevision">
        UPDATE rag_document
        SET active_version_id = #{versionId}, active_generation = #{generation},
            target_generation = NULL, status = 'ready', indexed_at = #{indexedAt},
            page_count = #{pageCount}, chunk_count = #{chunkCount},
            last_error_code = NULL, last_error_message = NULL, revision = revision + 1
        WHERE tenant_id = #{tenantId} AND kb_id = #{knowledgeBaseId}
          AND document_id = #{documentId} AND target_generation = #{generation}
          AND revision = #{expectedRevision} AND status = 'processing' AND deleted = 0
    </update>

    <update id="closeTargetGenerationByTenantAndRevision">
        UPDATE rag_document
        SET target_generation = NULL,
            status = CASE WHEN active_version_id IS NULL THEN 'failed' ELSE 'ready' END,
            revision = revision + 1
        WHERE tenant_id = #{tenantId} AND kb_id = #{knowledgeBaseId}
          AND document_id = #{documentId} AND target_generation = #{generation}
          AND revision = #{expectedRevision} AND status = 'processing' AND deleted = 0
    </update>

    <update id="markDeletedByTenantAndRevision">
        UPDATE rag_document
        SET active_version_id = NULL, active_generation = 0, target_generation = NULL,
            chunk_count = 0, status = 'deleted', revision = revision + 1
        WHERE tenant_id = #{tenantId} AND kb_id = #{knowledgeBaseId}
          AND document_id = #{documentId} AND revision = #{expectedRevision}
          AND status = 'deleting' AND deleted = 0
    </update>
</mapper>
