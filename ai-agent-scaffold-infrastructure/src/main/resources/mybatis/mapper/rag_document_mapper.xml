<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.IRagDocumentDao">

    <resultMap id="RagDocumentMap" type="cn.bugstack.ai.infrastructure.dao.po.RagDocumentPO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="visibility" property="visibility"/>
        <result column="kb_id" property="knowledgeBaseId"/>
        <result column="document_id" property="documentId"/>
        <result column="file_name" property="fileName"/>
        <result column="source_type" property="sourceType"/>
        <result column="source_uri" property="sourceUri"/>
        <result column="content_hash" property="contentHash"/>
        <result column="status" property="status"/>
        <result column="metadata" property="metadata"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="RagDocumentColumns">
        id, tenant_id, owner_user_id, visibility, kb_id, document_id, file_name, source_type, source_uri, content_hash, status, metadata, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.RagDocumentPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO rag_document (tenant_id, owner_user_id, visibility, kb_id, document_id, file_name, source_type, source_uri, content_hash, status, metadata)
        VALUES (#{tenantId}, #{ownerUserId}, #{visibility}, #{knowledgeBaseId}, #{documentId}, #{fileName}, #{sourceType}, #{sourceUri}, #{contentHash}, #{status}, #{metadata})
    </insert>

    <update id="updateById" parameterType="cn.bugstack.ai.infrastructure.dao.po.RagDocumentPO">
        UPDATE rag_document
        SET tenant_id = #{tenantId}, owner_user_id = #{ownerUserId}, visibility = #{visibility}, kb_id = #{knowledgeBaseId}, document_id = #{documentId}, file_name = #{fileName}, source_type = #{sourceType}, source_uri = #{sourceUri}, content_hash = #{contentHash}, status = #{status}, metadata = #{metadata}
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="queryById" resultMap="RagDocumentMap">
        SELECT <include refid="RagDocumentColumns"/>
        FROM rag_document
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="queryByDocumentId" resultMap="RagDocumentMap">
        SELECT <include refid="RagDocumentColumns"/>
        FROM rag_document
        WHERE document_id = #{documentId} AND deleted = 0
    </select>

    <select id="queryListByTenantId" resultMap="RagDocumentMap">
        SELECT <include refid="RagDocumentColumns"/>
        FROM rag_document
        WHERE tenant_id = #{tenantId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByOwnerUserId" resultMap="RagDocumentMap">
        SELECT <include refid="RagDocumentColumns"/>
        FROM rag_document
        WHERE owner_user_id = #{ownerUserId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByKnowledgeBaseId" resultMap="RagDocumentMap">
        SELECT <include refid="RagDocumentColumns"/>
        FROM rag_document
        WHERE kb_id = #{knowledgeBaseId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByDocumentId" resultMap="RagDocumentMap">
        SELECT <include refid="RagDocumentColumns"/>
        FROM rag_document
        WHERE document_id = #{documentId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByTenantIdAndVisibility" resultMap="RagDocumentMap">
        SELECT <include refid="RagDocumentColumns"/>
        FROM rag_document
        WHERE tenant_id = #{tenantId} AND visibility = #{visibility} AND deleted = 0
        ORDER BY id DESC
    </select>

</mapper>
