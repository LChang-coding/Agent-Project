<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.IAgentScheduleConfigDao">

    <!-- 调度配置以租户和 configId 隔离；版本条件负责阻止旧扫描器覆盖新 Cron。 -->
    <resultMap id="AgentScheduleConfigMap" type="cn.bugstack.ai.infrastructure.dao.po.AgentScheduleConfigPO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="owner_user_id" property="ownerUserId"/>
        <result column="run_as_user_id" property="runAsUserId"/>
        <result column="visibility" property="visibility"/>
        <result column="config_id" property="configId"/>
        <result column="agent_id" property="agentId"/>
        <result column="agent_name" property="agentName"/>
        <result column="task_type" property="taskType"/>
        <result column="task_payload" property="taskPayload"/>
        <result column="run_as_role_code" property="runAsRoleCode"/>
        <result column="cron_expr" property="cronExpr"/>
        <result column="timezone" property="timezone"/>
        <result column="enabled" property="enabled"/>
        <result column="status" property="status"/>
        <result column="misfire_policy" property="misfirePolicy"/>
        <result column="max_retries" property="maxRetries"/>
        <result column="config_hash" property="configHash"/>
        <result column="config_version" property="configVersion"/>
        <result column="last_reconciled_at" property="lastReconciledAt"/>
        <result column="metadata" property="metadata"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="AgentScheduleConfigColumns">
        id, tenant_id, owner_user_id, run_as_user_id, visibility, config_id, agent_id, agent_name,
        task_type, task_payload, run_as_role_code, cron_expr, timezone, enabled, status, misfire_policy,
        max_retries, config_hash, config_version, last_reconciled_at, metadata, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.AgentScheduleConfigPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO agent_schedule_config (tenant_id, owner_user_id, run_as_user_id, visibility, config_id,
            agent_id, agent_name, task_type, task_payload, run_as_role_code, cron_expr, timezone, enabled,
            status, misfire_policy, max_retries, config_hash, config_version, last_reconciled_at, metadata)
        VALUES (#{tenantId}, #{ownerUserId}, #{runAsUserId}, #{visibility}, #{configId}, #{agentId},
            #{agentName}, #{taskType}, #{taskPayload}, #{runAsRoleCode}, #{cronExpr}, #{timezone}, #{enabled},
            #{status}, #{misfirePolicy}, #{maxRetries}, #{configHash}, #{configVersion}, #{lastReconciledAt}, #{metadata})
    </insert>

    <update id="updateById" parameterType="cn.bugstack.ai.infrastructure.dao.po.AgentScheduleConfigPO">
        UPDATE agent_schedule_config
        SET tenant_id = #{tenantId}, owner_user_id = #{ownerUserId}, run_as_user_id = #{runAsUserId},
            visibility = #{visibility}, config_id = #{configId}, agent_id = #{agentId}, agent_name = #{agentName},
            task_type = #{taskType}, task_payload = #{taskPayload}, run_as_role_code = #{runAsRoleCode},
            cron_expr = #{cronExpr}, timezone = #{timezone}, enabled = #{enabled}, status = #{status},
            misfire_policy = #{misfirePolicy}, max_retries = #{maxRetries}, config_hash = #{configHash},
            config_version = #{configVersion}, last_reconciled_at = #{lastReconciledAt}, metadata = #{metadata}
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="queryById" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="queryByConfigId" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE config_id = #{configId} AND deleted = 0
    </select>

    <select id="queryListByTenantId" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE tenant_id = #{tenantId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByOwnerUserId" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE owner_user_id = #{ownerUserId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByRunAsUserId" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE run_as_user_id = #{runAsUserId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByConfigId" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE config_id = #{configId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByTenantIdAndVisibility" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE tenant_id = #{tenantId} AND visibility = #{visibility} AND deleted = 0
        ORDER BY id DESC
    </select>

    <!-- 长周期扫描只读取待协调配置；后续以 configHash 判断插入还是冲突更新。 -->
    <select id="queryForReconcile" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE deleted = 0
          AND (config_hash IS NULL OR config_hash = '' OR last_reconciled_at IS NULL
            OR update_time &gt; last_reconciled_at)
        ORDER BY id ASC
        LIMIT #{limit}
    </select>

    <select id="queryOwned" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE tenant_id &lt;=&gt; #{tenantId} AND owner_user_id = #{userId}
          AND config_id = #{configId} AND deleted = 0
    </select>

    <select id="queryOwnedList" resultMap="AgentScheduleConfigMap">
        SELECT <include refid="AgentScheduleConfigColumns"/>
        FROM agent_schedule_config
        WHERE tenant_id &lt;=&gt; #{tenantId} AND owner_user_id = #{userId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <update id="updateOwned" parameterType="cn.bugstack.ai.infrastructure.dao.po.AgentScheduleConfigPO">
        UPDATE agent_schedule_config
        SET agent_id = #{agentId}, agent_name = #{agentName}, task_type = #{taskType},
            task_payload = #{taskPayload}, run_as_role_code = #{runAsRoleCode}, cron_expr = #{cronExpr},
            timezone = #{timezone}, enabled = #{enabled}, status = #{status},
            misfire_policy = #{misfirePolicy}, max_retries = #{maxRetries},
            config_hash = NULL, last_reconciled_at = NULL
        WHERE tenant_id &lt;=&gt; #{tenantId} AND owner_user_id = #{ownerUserId}
          AND config_id = #{configId} AND deleted = 0
    </update>

    <update id="updateEnabled">
        UPDATE agent_schedule_config
        SET enabled = #{enabled}, status = #{status}, config_hash = NULL, last_reconciled_at = NULL
        WHERE tenant_id &lt;=&gt; #{tenantId} AND owner_user_id = #{userId}
          AND config_id = #{configId} AND deleted = 0
    </update>

    <!-- 仅在配置版本未漂移时记录协调结果，避免旧扫描覆盖新 Cron。 -->
    <update id="updateReconciled">
        UPDATE agent_schedule_config
        SET config_hash = #{configHash}, config_version = #{configVersion},
            last_reconciled_at = #{reconciledAt}, update_time = update_time
        WHERE config_id = #{configId} AND update_time &lt;=&gt; #{expectedUpdateTime} AND deleted = 0
    </update>

</mapper>
