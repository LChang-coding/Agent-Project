<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.IChatSessionDao">

    <resultMap id="ChatSessionMap" type="cn.bugstack.ai.infrastructure.dao.po.ChatSessionPO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="user_id" property="userId"/>
        <result column="session_id" property="sessionId"/>
        <result column="agent_id" property="agentId"/>
        <result column="agent_name" property="agentName"/>
        <result column="app_name" property="appName"/>
        <result column="title" property="title"/>
        <result column="status" property="status"/>
        <result column="last_message_time" property="lastMessageTime"/>
        <result column="context_revision" property="contextRevision"/>
        <result column="metadata" property="metadata"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="ChatSessionColumns">
        id, tenant_id, user_id, session_id, agent_id, agent_name, app_name, title, status, last_message_time, context_revision, metadata, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.ChatSessionPO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_session (tenant_id, user_id, session_id, agent_id, agent_name, app_name, title, status, last_message_time, context_revision, metadata)
        VALUES (#{tenantId}, #{userId}, #{sessionId}, #{agentId}, #{agentName}, #{appName}, #{title}, #{status}, #{lastMessageTime}, #{contextRevision}, #{metadata})
    </insert>

    <update id="updateById" parameterType="cn.bugstack.ai.infrastructure.dao.po.ChatSessionPO">
        UPDATE chat_session
        SET tenant_id = #{tenantId}, user_id = #{userId}, session_id = #{sessionId}, agent_id = #{agentId}, agent_name = #{agentName}, app_name = #{appName}, title = #{title}, status = #{status}, last_message_time = #{lastMessageTime}, context_revision = #{contextRevision}, metadata = #{metadata}
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="queryById" resultMap="ChatSessionMap">
        SELECT <include refid="ChatSessionColumns"/>
        FROM chat_session
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="queryBySessionId" resultMap="ChatSessionMap">
        SELECT <include refid="ChatSessionColumns"/>
        FROM chat_session
        WHERE session_id = #{sessionId} AND deleted = 0
    </select>

    <select id="queryByTenantUserSession" resultMap="ChatSessionMap">
        SELECT <include refid="ChatSessionColumns"/>
        FROM chat_session
        WHERE user_id = #{userId}
          AND session_id = #{sessionId}
          AND deleted = 0
        <choose>
            <when test="tenantId == null or tenantId == ''">
                AND (tenant_id IS NULL OR tenant_id = '')
            </when>
            <otherwise>
                AND tenant_id = #{tenantId}
            </otherwise>
        </choose>
        LIMIT 1
    </select>

    <select id="lockByTenantUserSession" resultMap="ChatSessionMap">
        SELECT <include refid="ChatSessionColumns"/>
        FROM chat_session
        WHERE user_id = #{userId}
          AND session_id = #{sessionId}
          AND deleted = 0
        <choose>
            <when test="tenantId == null or tenantId == ''">
                AND (tenant_id IS NULL OR tenant_id = '')
            </when>
            <otherwise>
                AND tenant_id = #{tenantId}
            </otherwise>
        </choose>
        LIMIT 1
        FOR UPDATE
    </select>

    <select id="queryPage" resultMap="ChatSessionMap">
        SELECT <include refid="ChatSessionColumns"/>
        FROM chat_session
        WHERE user_id = #{userId} AND status != 'deleted' AND deleted = 0
        <choose><when test="tenantId == null or tenantId == ''">AND (tenant_id IS NULL OR tenant_id = '')</when><otherwise>AND tenant_id = #{tenantId}</otherwise></choose>
        <if test="cursorTime != null">
            AND (last_message_time &lt; #{cursorTime}
                OR (last_message_time = #{cursorTime} AND session_id &lt; #{cursorSessionId}))
        </if>
        ORDER BY last_message_time DESC, session_id DESC
        LIMIT #{limit}
    </select>

    <update id="updateLastMessageTime">
        UPDATE chat_session
        SET last_message_time = #{lastMessageTime}
        WHERE user_id = #{userId}
          AND session_id = #{sessionId}
          AND deleted = 0
        <choose>
            <when test="tenantId == null or tenantId == ''">
                AND (tenant_id IS NULL OR tenant_id = '')
            </when>
            <otherwise>
                AND tenant_id = #{tenantId}
            </otherwise>
        </choose>
    </update>

    <update id="incrementContextRevision">
        UPDATE chat_session SET context_revision = context_revision + 1
        WHERE user_id = #{userId} AND session_id = #{sessionId} AND deleted = 0
        <choose><when test="tenantId == null or tenantId == ''">AND (tenant_id IS NULL OR tenant_id = '')</when><otherwise>AND tenant_id = #{tenantId}</otherwise></choose>
    </update>

    <select id="queryListByTenantId" resultMap="ChatSessionMap">
        SELECT <include refid="ChatSessionColumns"/>
        FROM chat_session
        WHERE tenant_id = #{tenantId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListByUserId" resultMap="ChatSessionMap">
        SELECT <include refid="ChatSessionColumns"/>
        FROM chat_session
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <select id="queryListBySessionId" resultMap="ChatSessionMap">
        SELECT <include refid="ChatSessionColumns"/>
        FROM chat_session
        WHERE session_id = #{sessionId} AND deleted = 0
        ORDER BY id DESC
    </select>

    <update id="softDelete">
        UPDATE chat_session SET status = 'deleted', deleted = 1
        WHERE user_id = #{userId} AND session_id = #{sessionId} AND deleted = 0
        <choose><when test="tenantId == null or tenantId == ''">AND (tenant_id IS NULL OR tenant_id = '')</when><otherwise>AND tenant_id = #{tenantId}</otherwise></choose>
    </update>

</mapper>
