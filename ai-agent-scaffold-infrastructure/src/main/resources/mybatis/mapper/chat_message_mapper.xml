<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bugstack.ai.infrastructure.dao.IChatMessageDao">

    <resultMap id="ChatMessageMap" type="cn.bugstack.ai.infrastructure.dao.po.ChatMessagePO">
        <id column="id" property="id"/>
        <result column="tenant_id" property="tenantId"/>
        <result column="user_id" property="userId"/>
        <result column="session_id" property="sessionId"/>
        <result column="message_id" property="messageId"/>
        <result column="run_id" property="runId"/>
        <result column="validity_status" property="validityStatus"/>
        <result column="invalid_reason" property="invalidReason"/>
        <result column="invalidated_at" property="invalidatedAt"/>
        <result column="role" property="role"/>
        <result column="content_type" property="contentType"/>
        <result column="content" property="content"/>
        <result column="estimated_token_count" property="estimatedTokenCount"/>
        <result column="sequence_no" property="sequenceNo"/>
        <result column="parent_message_id" property="parentMessageId"/>
        <result column="trace_id" property="traceId"/>
        <result column="metadata" property="metadata"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="ChatMessageColumns">
        id, tenant_id, user_id, session_id, message_id, run_id, validity_status, invalid_reason, invalidated_at, role, content_type, content, estimated_token_count, sequence_no, parent_message_id, trace_id, metadata, create_time, update_time, deleted
    </sql>

    <insert id="insert" parameterType="cn.bugstack.ai.infrastructure.dao.po.ChatMessagePO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_message (tenant_id, user_id, session_id, message_id, run_id, validity_status, invalid_reason, invalidated_at, role, content_type, content, estimated_token_count, sequence_no, parent_message_id, trace_id, metadata)
        VALUES (#{tenantId}, #{userId}, #{sessionId}, #{messageId}, #{runId}, #{validityStatus}, #{invalidReason}, #{invalidatedAt}, #{role}, #{contentType}, #{content}, #{estimatedTokenCount}, #{sequenceNo}, #{parentMessageId}, #{traceId}, #{metadata})
    </insert>

    <update id="updateById" parameterType="cn.bugstack.ai.infrastructure.dao.po.ChatMessagePO">
        UPDATE chat_message
        SET tenant_id = #{tenantId}, user_id = #{userId}, session_id = #{sessionId}, message_id = #{messageId}, run_id = #{runId}, validity_status = #{validityStatus}, invalid_reason = #{invalidReason}, invalidated_at = #{invalidatedAt}, role = #{role}, content_type = #{contentType}, content = #{content}, estimated_token_count = #{estimatedTokenCount}, sequence_no = #{sequenceNo}, parent_message_id = #{parentMessageId}, trace_id = #{traceId}, metadata = #{metadata}
        WHERE id = #{id} AND deleted = 0
    </update>

    <select id="queryById" resultMap="ChatMessageMap">
        SELECT <include refid="ChatMessageColumns"/>
        FROM chat_message
        WHERE id = #{id} AND deleted = 0
    </select>

    <select id="queryByMessageId" resultMap="ChatMessageMap">
        SELECT <include refid="ChatMessageColumns"/>
        FROM chat_message
        WHERE message_id = #{messageId} AND deleted = 0
    </select>

    <select id="queryListByTenantId" resultMap="ChatMessageMap">
        SELECT <include refid="ChatMessageColumns"/>
        FROM chat_message
        WHERE tenant_id = #{tenantId} AND deleted = 0
        ORDER BY sequence_no ASC, id ASC
    </select>

    <select id="queryListByUserId" resultMap="ChatMessageMap">
        SELECT <include refid="ChatMessageColumns"/>
        FROM chat_message
        WHERE user_id = #{userId} AND deleted = 0
        ORDER BY sequence_no ASC, id ASC
    </select>

    <select id="queryListBySessionId" resultMap="ChatMessageMap">
        SELECT <include refid="ChatMessageColumns"/>
        FROM chat_message
        WHERE session_id = #{sessionId} AND deleted = 0
        ORDER BY sequence_no ASC, id ASC
    </select>

    <select id="queryMaxSequenceNo" resultType="java.lang.Integer">
        SELECT COALESCE(MAX(sequence_no), 0)
        FROM chat_message
        WHERE user_id = #{userId}
          AND session_id = #{sessionId}
          AND deleted = 0
        <choose>
            <when test="tenantId == null or tenantId == ''">
                AND (tenant_id IS NULL OR tenant_id = '')
            </when>
            <otherwise>
                AND tenant_id = #{tenantId}
            </otherwise>
        </choose>
    </select>

    <select id="queryContextRange" resultMap="ChatMessageMap">
        SELECT <include refid="ChatMessageColumns"/>
        FROM chat_message
        WHERE user_id = #{userId}
          AND session_id = #{sessionId}
          AND sequence_no &gt; #{fromSequenceExclusive}
          AND sequence_no &lt;= #{toSequenceInclusive}
          AND validity_status = 'active'
          AND deleted = 0
        <choose>
            <when test="tenantId == null or tenantId == ''">
                AND (tenant_id IS NULL OR tenant_id = '')
            </when>
            <otherwise>
                AND tenant_id = #{tenantId}
            </otherwise>
        </choose>
        ORDER BY sequence_no ASC, id ASC
    </select>

    <select id="sumContextTokens" resultType="java.lang.Integer">
        SELECT COALESCE(SUM(COALESCE(estimated_token_count, CHAR_LENGTH(content))), 0)
        FROM chat_message
        WHERE user_id = #{userId}
          AND session_id = #{sessionId}
          AND sequence_no &gt; #{fromSequenceExclusive}
          AND sequence_no &lt;= #{toSequenceInclusive}
          AND validity_status = 'active'
          AND deleted = 0
        <choose>
            <when test="tenantId == null or tenantId == ''">
                AND (tenant_id IS NULL OR tenant_id = '')
            </when>
            <otherwise>
                AND tenant_id = #{tenantId}
            </otherwise>
        </choose>
    </select>

    <update id="invalidateRunMessages">
        UPDATE chat_message
        SET validity_status = 'invalidated', invalid_reason = #{reason}, invalidated_at = #{invalidatedAt}
        WHERE user_id = #{userId} AND session_id = #{sessionId} AND run_id = #{runId}
          AND validity_status = 'active' AND deleted = 0
        <choose><when test="tenantId == null or tenantId == ''">AND (tenant_id IS NULL OR tenant_id = '')</when><otherwise>AND tenant_id = #{tenantId}</otherwise></choose>
    </update>

    <select id="queryRunMessages" resultMap="ChatMessageMap">
        SELECT <include refid="ChatMessageColumns"/> FROM chat_message
        WHERE user_id = #{userId} AND session_id = #{sessionId} AND run_id = #{runId} AND deleted = 0
        <choose><when test="tenantId == null or tenantId == ''">AND (tenant_id IS NULL OR tenant_id = '')</when><otherwise>AND tenant_id = #{tenantId}</otherwise></choose>
        ORDER BY sequence_no ASC, id ASC
    </select>

</mapper>
