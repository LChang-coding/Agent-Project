# 触发器层、领域层、基建层逐行语义注释闭环计划

## 一、目标

在不改变运行逻辑、接口协议、数据库语义和依赖关系的前提下，为以下三个生产模块补齐高信息密度中文注释：

- `ai-agent-scaffold-trigger`
- `ai-agent-scaffold-domain`
- `ai-agent-scaffold-infrastructure`

完成后，读者应能直接从代码理解请求入口、领域决策、状态变化、外部副作用、持久化约束、异常后果、租户隔离、并发控制、取消闸门和链路追踪。

## 二、注释标准

1. 每个生产类说明职责、输入输出、所属层和明确边界。
2. 每个方法说明业务动作、参数语义、返回结果、主要副作用和异常后果。
3. 每段有效业务代码说明“为什么执行”和“执行后改变什么”，不机械复述 Java 语法。
4. 条件分支说明命中条件对应的业务后果。
5. 数据库、消息、网络、文件、模型、工具调用必须标明外部副作用和失败边界。
6. 并发代码必须说明线程归属、共享状态、取消检查、幂等或竞争控制。
7. 身份相关代码必须说明 `tenantId`、`userId`、`sessionId`、`runId`、`traceId` 的来源与隔离用途。
8. 不为 `package`、普通 `import`、花括号、空行、显然的 getter/setter 和链式调用的每个物理行制造无意义注释。
9. XML Mapper 对查询目的、租户条件、软删除、锁、乐观锁、幂等更新和状态推进进行语义注释。
10. 不修改业务行为；发现缺陷只记录到计划文档，未经授权不顺手修复。

## 三、范围

### 纳入

- 三个模块的 `src/main/java`
- 基建层 `src/main/resources` 中由本项目维护的 Mapper、配置和运行资源

### 排除

- `target`、构建产物、日志、缓存、对象存储数据
- 第三方源码和自动生成文件
- 测试源码；测试只作为编译与行为回归证据，本轮不做逐行注释
- 当前工作区已有且与本目标无关的修改和未跟踪文件

## 四、执行阶段

### 阶段 0：基线与覆盖清单

- 完整读取 `codex.md`。
- 识别 Spring Boot 版本、模块依赖和现有注释规范。
- 统计三个模块生产文件、代码行数、Java 类型和 Mapper 数量。
- 生成逐文件覆盖清单，记录初始 Git 状态。

验收：

- 范围清单可复核。
- 无关工作区修改有明确记录且后续不纳入提交。

### 阶段 1：触发器层

- Controller、Job、Listener、RPC/HTTP 适配入口。
- 请求身份、参数转换、响应、SSE、异常映射和 Trace 传播。

验收：

- 触发器层生产文件全部覆盖。
- 模块编译通过。
- 计划文档追加实际操作与验证结果。
- 使用中文提交信息完成本地提交。

### 阶段 2：领域层

- Agent、工作流、会话、运行控制、上下文压缩、RAG、工具、模型用量等领域链路。
- 重点解释状态机、不变量、取消检查、并行行为和副作用边界。

验收同阶段 1，并补充领域层相关单元测试。

### 阶段 3：基建层

- Repository、DAO、PO、MyBatis Mapper、外部客户端、消息和存储实现。
- 重点解释持久化映射、SQL 条件、锁、幂等、重试和基础设施失败边界。

验收同阶段 1，并补充 Mapper/XML 解析与基建层测试。

### 阶段 4：全量验收

- 全量编译。
- 可执行的单元测试和集成测试。
- 注释覆盖审计：逐文件检查类、方法、复杂分支、外部副作用和 SQL。
- 检查注释是否与代码一致、是否存在复述语法或误导性说明。
- 检查 Git diff，确认没有业务逻辑变化和无关文件混入。
- 追加最终操作记录并完成中文本地提交。

## 五、风险与控制

1. **范围巨大**：按层级和业务包拆分小批次，避免一次性产生不可审查 diff。
2. **注释污染可读性**：以语义覆盖为标准，不按物理行机械堆叠。
3. **误改逻辑**：只使用注释补丁；每批通过 `git diff --word-diff` 和编译检查。
4. **注释失真**：从调用链、实体和 SQL 交叉验证后再描述，不凭方法名猜测。
5. **已有脏工作区**：只暂存本计划和本目标修改，禁止 `git add .`。

## 六、操作记录

### 2026-07-26：计划建立

- 已明确三个模块的注释目标、范围、排除项、分层执行顺序和验收门禁。
- 已确定采用“每段有效业务代码均有准确语义解释”的标准，避免为花括号、导入和显然语句添加噪声。
- 已完整读取 `codex.md`，确认项目为 Java 17 + Spring Boot 模块化单体，注释必须服从现有分层、租户隔离、ToolGateway 和中文提交规范。
- 已建立生产代码基线：
  - 触发器层：23 个 Java 文件，3,719 行；其中 HTTP 17 个、Job 4 个、Listener 2 个。
  - 领域层：281 个 Java 文件，23,531 行；主要包括 RAG 79 个、Agent 47 个、Tool 35 个、Context 27 个、Workflow 18 个等。
  - 基建层：130 个 Java 文件，15,508 行；另有 36 个 MyBatis Mapper XML。
  - 总计：434 个 Java 文件、42,758 行 Java 生产代码、36 个 Mapper XML。
- 已记录初始脏工作区：运行日志、对象存储目录、既有 RAG 文档、设计文档、Skill 等均为用户已有或运行产生内容，本目标禁止暂存和提交这些文件。
- 已确认 `plan-orchestrate` 仅用于生成外部编排提示词，不适用于当前直接修改代码；后续使用本计划和本地验收门禁推进。
- 下一步生成触发器层逐文件清单，按 HTTP、Job、Listener 三批补齐注释。

### 2026-07-26：触发器层第一批执行计划

范围：

- `trigger/job` 下 3 个实现类和包说明。
- `trigger/listener/ContextCompactionConsumer` 和包说明。
- 小型 HTTP 入口：`AgentConfigController`、`RunControlController`、`RagKnowledgeBaseController`、`RagKnowledgeBaseDeletionController`、`RagRetrievalDebugController`。

本批重点：

- 说明 XXL-JOB 与本地兜底调度的互斥和触发边界。
- 说明 Kafka 压缩消费者的主消息、重试、死信和幂等处理语义。
- 说明控制器如何从可信上下文取得身份，以及为何只做协议转换、不承载领域规则。
- 补齐所有业务方法契约，并为关键参数转换、状态判断和异常后果增加短注释。

本批门禁：

- 只产生注释和计划文档修改，不改变可执行语句。
- `ai-agent-scaffold-trigger` 编译通过。
- 通过 diff 审计确认没有混入日志和现有无关文件。

### 2026-07-26：触发器层第一批操作记录

- 已完成 9 个实现类的语义注释：
  - 调度：`ScheduleLocalFallback`、`ScheduleXxlJobHandler`、`XxlJobConfiguration`。
  - 消费：`ContextCompactionConsumer`。
  - HTTP：`AgentConfigController`、`RunControlController`、`RagKnowledgeBaseController`、`RagKnowledgeBaseDeletionController`、`RagRetrievalDebugController`。
- 注释已经覆盖调度唤醒与业务账本边界、Kafka CAS 认领与线程上下文清理、JWT 身份来源、乐观锁、防误删、运行取消/引导、RAG 调试 Trace 关联和 DTO 转换边界。
- 使用零上下文 diff 审计新增行，除注释和计划记录外没有新增可执行语句。
- 执行 `mvn -pl ai-agent-scaffold-trigger -am -DskipTests compile`，结果 `BUILD SUCCESS`。
- Maven 仍报告 `ai-agent-scaffold-api` 的 `parent.relativePath` 指向不同 groupId，以及旧版 resources plugin 不识别 `propertiesEncoding`；这是既有构建告警，本批未修改。
- 下一批处理剩余 HTTP 控制器中的 RAG 配置/文档、资产、会话洞察和定时任务入口。

### 2026-07-26：触发器层第二批执行计划

范围：

- `RagRetrievalConfigurationController`
- `RagDocumentController`
- `AssetController`
- `SessionInsightController`
- `ScheduleController`

本批重点：

- 说明 RAG Profile、绑定和会话选择的权限与乐观锁边界。
- 说明文档上传、摄取任务、取消和重试的异步语义。
- 说明附件元数据与对象存储访问边界。
- 说明会话统计只读取数据库事实，不从浏览器缓存推断。
- 说明调度配置、任务和执行记录的协议映射。

门禁与第一批相同：只改注释、模块编译通过、diff 不混入业务逻辑和无关文件。

### 2026-07-26：触发器层第二批操作记录

- 已完成 `RagRetrievalConfigurationController`、`RagDocumentController`、`AssetController`、`SessionInsightController`、`ScheduleController` 的语义注释。
- 已明确：
  - 检索策略和绑定的强类型转换、管理员权限与乐观锁边界。
  - Multipart 暂存文件生命周期、JWT 身份注入、异步摄取/删除/取消/重试语义。
  - 资产归属先校验后下载、游标分页多取一条和 MIME 降级策略。
  - 上下文与 Token 统计来自服务端账本，不使用浏览器估值。
  - 手动触发只登记任务，Agent 不在 HTTP 线程执行；调度执行身份固定为当前 JWT 用户。
- 零上下文 diff 审计未发现新增可执行语句。
- 再次执行 `mvn -pl ai-agent-scaffold-trigger -am -DskipTests compile`，结果 `BUILD SUCCESS`。
- 下一批处理 `SessionController`、`SessionShareController`、`AuthController`、`WorkflowController`、`ToolController`、`AgentServiceController` 及三个包说明，完成触发器层整体闭环。

### 2026-07-26：触发器层第三批执行计划

范围：

- `SessionController`
- `SessionShareController`
- `AuthController`
- `WorkflowController`
- `ToolController`
- `AgentServiceController`
- `trigger/http`、`trigger/job`、`trigger/listener` 的包说明

本批重点：

- 会话创建、数据库历史读取、删除和 RAG 会话选择。
- 分享快照、工具权限差异提醒、导入和下载边界。
- 认证 Cookie/JWT、租户上下文与敏感信息响应边界。
- 工作流草稿、发布、运行和版本选择。
- ToolGateway 管理面与实际调用面的职责区分。
- Agent 普通/SSE 对话、附件绑定、runId/traceId 返回、取消监听及错误事件。

门禁：

- 审计 6 个大型控制器的全部方法契约和复杂分支。
- 三个包说明准确描述适配器边界。
- 编译与触发器相关测试通过后，追加操作记录并中文提交。

### 2026-07-26：触发器层第三批操作记录

- 已完成 6 个大型控制器和 3 个包说明的注释审计与补齐。
- `AgentServiceController` 已明确：
  - `message` 经 `ChatService` 保存后进入普通 ADK Agent 或工作流 DAG。
  - 同步链如何汇总 Event/文本，SSE 链如何依次返回 trace/session/run/message/citation_validation。
  - run 创建与取消句柄注册的竞态封闭、附件ID传递和最终引用快照读取时机。
- `SessionController`、`SessionShareController` 已补足游标分页、数据库历史、RAG 会话快照、引用原文、分享快照、工具权限预检和独立导入语义。
- `AuthController`、`WorkflowController`、`ToolController` 已明确认证领域边界、工作流图转换边界，以及 Tool 管理面与 ToolGateway 运行面的区分。
- 注释覆盖统计：触发器层 23 个 Java 文件均有文件/类说明；20 个实现类的每个声明方法均有对应 Javadoc，复杂分支另有原因注释。
- 执行 `mvn -pl ai-agent-scaffold-trigger -am -DskipTests clean compile`，23 个触发器源文件及上游模块从干净目录重新编译成功。
- 首次运行 28 个相关测试时，Java 25 超出 Byte Buddy 1.15.11 官方支持范围，18 个 Mockito inline mock 在测试初始化阶段报错；根因不是代码失败。
- 使用 `-Dnet.bytebuddy.experimental=true` 适配当前 Java 25 后重新执行相同 28 个测试：`Tests run: 28, Failures: 0, Errors: 0, Skipped: 0`，`BUILD SUCCESS`。
- 触发器层阶段完成。下一阶段进入领域层，先按包和类复杂度建立小批次覆盖清单。

### 2026-07-26：领域层第一批执行计划（调度与会话分享）

范围：

- `domain/schedule` 下 12 个 Java 文件。
- `domain/share` 下 9 个 Java 文件。

本批重点：

- 调度领域：解释配置同步、Cron 解析、一致性哈希、任务认领、分片执行、下一次触发时间推进、失败重试和本地/XXL-JOB 触发器边界。
- 会话分享：解释不可变导出快照、对象存储交付、工具依赖收集、接收方权限预检、导入后身份重建和分享有效期。
- 模型与端口：说明字段所代表的领域事实、状态值约束及仓储方法的原子性要求。
- 服务：为所有公开和私有业务方法补齐契约，并在非显然分支旁说明“为什么这样处理”。

门禁：

- 只新增或修订注释，不改变可执行语句、配置和数据结构。
- 21 个文件逐一通过类说明、方法说明和复杂分支覆盖审计。
- `ai-agent-scaffold-domain` 干净编译通过，并运行现有调度/分享相关测试。
- 将真实操作与测试结果追加到本节后，再以中文信息本地提交。

### 2026-07-26：领域层第一批操作记录（调度与会话分享）

- 已完成 `domain/schedule` 12 个文件和 `domain/share` 9 个文件，共 21 个 Java 文件的语义注释。
- 调度领域已明确：
  - 配置摘要只纳入执行语义字段，稳定业务键不随 Cron 变化，从而通过冲突更新避免重复任务。
  - 数据库短租约负责故障接管，递增栅栏令牌负责拒绝过期 Worker 回写。
  - 同一触发键先幂等登记执行；命中成功或终止记录时只推进游标，不再次调用 Agent。
  - `skip`、`fire_once_now`、`catch_up` 三类错过策略的触发点处理和下一时间计算边界。
  - 固化执行身份只在 Worker 线程临时恢复，并在成功、失败和中断路径统一清理。
- 会话分享领域已明确：
  - 原令牌只返回一次，数据库仅保存摘要；对象存储快照使用内容摘要验真。
  - 导出协议只允许会话白名单元数据及人机纯文本，不复制源租户、主键、附件地址和内部事件。
  - 分享额度、接收方幂等导入和新会话创建在分享行锁与事务内完成。
  - 工具依赖由服务端运行证据生成，接收方按工具标识与版本逐项预检；风险需显式确认。
  - 导入通过会话领域方法重建所有权和消息，不复用源消息身份。
- 覆盖审计：
  - 本批 79 个显式声明方法均有契约说明。
  - 零上下文 diff 未发现任何新增可执行语句；`git diff --check` 对本批路径通过。
- 执行 `mvn -pl ai-agent-scaffold-domain -am -DskipTests clean compile`，281 个领域层源文件干净编译成功。
- 使用 Java 25 兼容参数运行 `CronScheduleSupportTest`、`ScheduleDispatcherTest`、`ScheduleReconcilerTest`、`SessionShareServiceTest`：共 12 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。
- 测试中的一次 `InterruptedException` 警告由 `ScheduleDispatcherTest` 主动中断 Worker 验证关闭语义产生，断言通过，不是产品异常。

### 2026-07-26：领域层第二批执行计划（运行控制与会话）

范围：

- `domain/run` 下 10 个 Java 文件。
- `domain/session` 下 7 个 Java 文件。

本批重点：

- 运行控制：解释 run 状态机、取消/引导的版本推进、活动运行互斥、工具调用前门禁、快照缓存与数据库事实的关系。
- 会话领域：解释会话所有权、数据库历史游标、消息序号、无效消息过滤、删除会话前的关联资源清理，以及工作流会话身份。
- 跨域一致性：明确“运行被取消”如何阻止后续工具副作用，以及被取消消息为何不能进入会话历史、上下文压缩和分享快照。

门禁：

- 17 个文件逐一审计类职责、字段语义、方法契约和状态分支。
- diff 中只允许注释和计划记录，不得改变运行状态机或查询条件。
- 领域层干净编译通过，并运行现有 run/session 相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第四批操作记录（RAG 端口与文档中间表示）

- 已逐一审计 7 个 RAG 外部能力端口、5 个仓储端口和 3 个文档中间表示文件；原有准确注释保留，仅补齐缺口。
- 外部能力边界已明确：
  - Embedding 的 query/passage 输入语义、批次顺序、固定维度和可审计模型版本。
  - 文档解析只接受受控工作目录路径；OCR 的禁用、自动和强制模式；旧 Markdown 结果只能提升为最小可追溯 IR。
  - 检索审计主记录与最终引用必须原子留痕。
- 仓储边界已明确：
  - 业务方法必须以可信 tenantId 为首参；全局 Worker 扫描只返回最小候选投影。
  - 删除任务按 revision、租约和 fencing token 三重条件更新；零残留验证后才允许关闭知识库。
  - 上传、文档删除和知识库删除的登记均以本地数据库事务建立屏障。
- 文档 IR 已明确块类型、清洗/风险标记、质量严重度和 READY/WARNING/REVIEW/REJECTED 处置语义；原文、规范化文本、版面坐标、表格结构和清洗轨迹必须同时保留。
- 零上下文 diff 审计未发现新增可执行语句；本批路径 `git diff --check` 通过。
- 执行领域层干净编译成功；运行解析协议、IR 清洗、质量门禁、Sparse、Qdrant、检索审计及仓储契约等 10 个测试类：共 73 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第五批执行计划（RAG 实体与值对象）

范围：

- `domain/rag/model/entity` 下 20 个 Java 文件。
- `domain/rag/model/valobj` 下 22 个 Java 文件。

本批重点：

- 知识库、逻辑文档、不可变版本、分块、摄取任务和删除任务的身份、状态、revision、generation、租约、栅栏与 checkpoint。
- 检索 Profile、绑定、请求、候选、引用、审计和失败语义。
- 值对象构造校验、不可变集合、防御复制和状态迁移含义。

门禁：

- 42 个文件逐一审计，只补充有业务价值的注释，不改变构造、校验或状态机。
- 零上下文 diff 不得出现可执行语句变化。
- 领域层干净编译和相关模型/状态机测试通过，追加记录后中文提交。

### 2026-07-26：领域层第五批操作记录（RAG 实体与值对象）

- 已逐一审计 `model/entity` 20 个文件和 `model/valobj` 22 个文件，保留既有准确状态机说明，并补齐私有校验、复制迁移、对象键和枚举语义。
- 已明确三类并发版本：
  - `revision` 用于数据库乐观条件更新。
  - `lease` 用于 Worker 故障接管与过期判断。
  - `fencingToken` 单调递增，用于阻止失去租约的旧 Worker 继续产生外部副作用或提交结果。
- 已明确摄取与删除进度：
  - 摄取 checkpoint 记录阶段、分块、Embedding 批次、向量写入和不可变解析产物事实，所有进度只能单调推进。
  - 知识库删除 checkpoint 记录子文档总数与完成数，WAITING 会主动释放租约等待子任务，不计为失败。
  - 文档删除顺序固定为向量、业务分块、源文件；知识库只有完成零残留验证后才能关闭墓碑。
- 已明确检索模型：
  - Dense/Sparse/Hybrid 模式、NONE/RRF/WEIGHTED 融合、Profile 候选数与预算约束。
  - 检索结果只暴露公开引用身份与阶段指标；管理员诊断不包含向量、对象键和正文。
  - 最终回答引用状态区分未使用 RAG、证据未引用、合法引用和伪造/越界引用。
- 已明确对象存储键由租户、知识库、文档和版本共同限定，路径段拒绝分隔符与穿越，长作用域标识使用稳定短摘要。
- 零上下文 diff 审计未发现新增可执行语句；本批路径 `git diff --check` 通过。
- 执行领域层干净编译成功；运行摄取状态机、删除状态机、索引激活和回答引用等 6 个测试类：共 31 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第六批执行计划（RAG 文档预处理算法）

范围：

- `DeterministicSparseEncoder`
- `DocumentIrCleaner`
- `DocumentParseQualityEvaluator`
- `DocumentIrChunker`
- `StructuredRagChunker`
- `RagUploadFilePolicy`

本批重点：

- 上传文件名、扩展名、MIME、Magic、路径、文件大小和压缩炸弹防护。
- IR 清洗中的 Unicode、控制字符、断词、重复页眉页脚、重复块、提示词注入、敏感内容与抑制可逆性。
- 解析覆盖率、阅读顺序、OCR、表格、重复率和替换字符质量门禁。
- 标题路径、表格行、父子块、相邻块、Token 预算和稳定 chunkId/contentHash。
- 确定性 Sparse 词项与权重，避免把 Dense 冒充混合检索。

门禁：

- 6 个算法文件逐方法和分支审计，只改注释。
- 领域层干净编译，并运行上传策略、清洗、质量、切块和 Sparse 测试。
- 追加真实记录后中文提交。

### 2026-07-26：领域层第三批操作记录（上下文管理）

- 已逐一审计 `domain/context` 27 个 Java 文件，补齐模型字段、策略参数、服务依赖、私有算法和关键并发分支注释。
- 上下文组装已明确：
  - 长期摘要、最近对话、附件、工作流上游和 RAG 统一进入候选集，再按类别优先级和模型总预算选择完整片段。
  - 有效历史只读取摘要覆盖点之后、本次可见序号之前的数据库消息；预览复用生产口径但不写缓存。
  - RAG 已开启但 Context Manager 或 RAG 预算不可用时显式失败，不允许静默退化。
- 压缩链路已明确：
  - 助手消息保存后只在未覆盖 Token 越过阈值时创建 MySQL 幂等任务，Kafka 仅负责即时通知。
  - 工具调用前重复执行阈值检测；当前线程抢到任务则同步压缩，其他消费者已领取时等待同一任务终态，压缩后要求模型重新推理。
  - 压缩输入用会话上下文版本和覆盖指纹锁定；指纹同时覆盖消息身份、序号、有效性、正文和附件文本。
  - 模型调用后、摘要激活前分别复核任务状态、栅栏、上下文版本、覆盖指纹和摘要基线版本。
  - 摘要 CAS 激活、任务完成和上下文版本推进在同一短事务内完成，模型调用不占用数据库事务。
- 取消/引导污染防护已明确：未完成重叠任务转为 stale；已完成摘要若覆盖失效消息则作废并恢复安全祖先；缓存仅在事务提交后清理。
- 零上下文 diff 审计确认除恢复多行枚举的临时编辑外，最终差异只包含注释；本批路径 `git diff --check` 通过。
- 执行 `mvn -pl ai-agent-scaffold-domain -am -DskipTests clean compile`，281 个领域层源文件干净编译成功。
- 使用 Java 25 兼容参数运行上下文、RAG/附件贡献、消费者 Trace、洞察和快照映射等 9 个测试类：共 24 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第四批执行计划（RAG 端口与文档中间表示）

范围：

- `domain/rag/adapter/port` 下 7 个 Java 文件。
- `domain/rag/adapter/repository` 下 5 个 Java 文件。
- `domain/rag/model/document` 下 3 个 Java 文件。

本批重点：

- 明确 Qdrant、Embedding、Sparse、Rerank、对象解析等外部能力的领域端口输入输出和失败边界。
- 明确知识库、文档、版本、摄取任务、检索配置等仓储方法的租户隔离、幂等、乐观锁和原子更新要求。
- 明确 DOCX、PDF、Markdown 解析后中间表示如何保留段落、表格、标题、页码和来源定位，避免过早压平为纯文本。

门禁：

- 15 个文件逐一审计，只允许注释变化。
- 领域层干净编译通过，并运行文档中间表示、清洗、切块或端口契约相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第二批操作记录（运行控制与会话）

- 已逐一审计 `domain/run` 10 个文件和 `domain/session` 7 个文件，并补齐缺失的类、字段、私有方法及关键状态分支注释。
- 运行控制已明确：
  - run 的 `CREATED` 到终态状态语义、乐观锁版本和上下文版本各自解决的问题。
  - 引导会先失效旧运行消息和上下文派生状态，再创建继承同一 trace 与固化 RAG 策略的后继运行。
  - 取消会在同一事务链中失效消息、撤销压缩派生状态、推进上下文版本、取消运行中用量，再于提交后中断本机流。
  - 工具回调前先检查取消、再按阈值压缩、压缩后要求模型重推；真正外发工具前锁数据库进行最终授权。
  - 极短 TTL 快照只服务无副作用高频检查，外部工具副作用绝不依赖缓存；事务提交后才失效快照。
- 会话领域已明确：
  - 会话是所有权、运行目标、RAG 策略和上下文版本的聚合根。
  - 锁会话后分配消息序号，保证并发消息顺序唯一；所有历史、分享和上下文查询统一只读有效消息。
  - 删除会话前先取消全部活动运行，再撤销上下文派生状态和分享授权，最后软删除会话。
  - RAG 策略通过版本条件更新，防止多标签页或并发请求静默覆盖。
- 零上下文 diff 审计确认本批未新增或修改可执行语句；本批路径 `git diff --check` 通过。
- 执行 `mvn -pl ai-agent-scaffold-domain -am -DskipTests clean compile`，281 个领域层源文件干净编译成功。
- 使用 Java 25 兼容参数运行 `RunStateSnapshotCacheTest`、`RunControlServiceTest`、`RunExecutionGateTest`、`SessionControllerTest`、`SessionDomainTest`、`SessionLifecycleServiceTest`：共 25 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第三批执行计划（上下文管理）

范围：

- `domain/context` 下 27 个 Java 文件。

本批重点：

- Token 计数、短期窗口选取、附件占用和 Context Manager 统计来源。
- 发送后与工具调用前的压缩门禁、任务幂等键、Kafka 异步状态机和同步工具前压缩。
- 压缩摘要覆盖范围、基线版本、消息有效性，以及取消/引导后对 pending、running、completed 压缩的回滚或失效。
- 运行上下文版本如何阻止旧提示词继续调用模型或工具。

门禁：

- 27 个文件逐一审计类、字段、端口、方法和复杂分支。
- 只改注释；零上下文 diff 不得出现可执行语句变化。
- 领域层干净编译通过，并运行现有 context/compaction 相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第六批操作记录（RAG 文档预处理算法）

- 已逐一审计 6 个算法文件，只补充职责、约束、失败边界和关键分支原因注释：
  - `RagUploadFilePolicy` 明确声明长度与磁盘实长双校验、受控普通文件、文件名归一化、MIME 一致性、Magic、DOCX 路径穿越与压缩炸弹预算，以及校验结束后的长度复核。
  - `DocumentIrCleaner` 明确可逆清洗、正文与表格一致处理、版面元素半数页阈值、重复块只抑制不删除，以及提示注入和敏感信息只标记不改写原文。
  - `DocumentParseQualityEvaluator` 明确来源覆盖、阅读顺序、OCR、表格、重复内容和替换字符六维评分，以及拒绝、人工复核、带警告可用和直接可用的裁决顺序。
  - `DocumentIrChunker` 与 `StructuredRagChunker` 明确结构边界、表头重复、父子预算、自然断句、Unicode 代理对、展示文本与 Embedding 文本分离、稳定 ID 和相邻子块关系。
  - `DeterministicSparseEncoder` 明确 CJK 单字/双字、英文词项、log-TF、哈希碰撞合并、L2 归一化和算法版本隔离。
- 零上下文 diff 审计未发现任何新增可执行语句；6 个文件 `git diff --check` 通过。
- 执行 `mvn -pl ai-agent-scaffold-domain -am -DskipTests clean compile`，281 个领域层源文件干净编译成功。
- 使用 Java 25 兼容参数运行 6 个测试类：`DocumentPreprocessingPipelineTest`、`DocumentIrCleanerTest`、`DocumentParseQualityEvaluatorTest`、`DeterministicSparseEncoderTest`、`StructuredRagChunkerTest`、`RagUploadFilePolicyTest`；共 37 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第七批执行计划（RAG 管理、授权与证据）

范围：

- 知识库与文档的创建、更新、上传、删除和租户授权服务。
- Agent/Workflow 绑定目标授权、会话 RAG 设置和检索配置管理。
- 回答引用元数据、引用校验、调用证据存储和工作流证据血缘。

本批重点：

- 明确租户、用户、角色与目标绑定的授权边界，禁止只凭前端传入 ID 操作资源。
- 明确上传受理、幂等任务、Outbox、版本激活、取消和删除补偿顺序。
- 明确知识库删除时绑定停用、任务终止、向量/分块/对象清理和状态推进。
- 明确回答引用必须来自本次运行的真实检索证据，工作流跨节点只能传播有血缘的证据。
- 明确会话开关与 Agent/Workflow 绑定策略如何固化到运行快照。

门禁：

- 逐类、逐方法和关键状态分支审计，只允许注释变化。
- 领域层干净编译，并运行知识库、文档、授权、会话 RAG、引用和证据相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第七批操作记录（RAG 管理、授权与证据）

- 已审计 13 个服务文件；其中 12 个补齐注释，`RagWorkflowEvidenceLineage` 原有注释已覆盖其全部公开规则，无需制造重复注释。
- 管理与授权边界已明确：
  - 知识库、文档、检索策略和绑定均先从可信租户范围读取实体，再校验管理员或目标可用性；跨租户资源统一按不存在处理。
  - Agent 绑定要求平台静态注册且租户启用；Workflow 绑定要求属于当前租户、已发布且有有效发布版本。
  - 会话 RAG 使用 `OFF/AUTO/MANUAL` 三态；更新时锁会话、校验 revision 和有效绑定，运行开始时再固化不可漂移快照。
- 摄取与删除闭环已明确：
  - 上传先校验文件并写对象存储，再原子登记文档、版本、任务和 Outbox；幂等命中或登记失败时补偿删除本次对象。
  - 文档删除用租户和逻辑文档生成稳定幂等键，原子建立文档/版本墓碑与删除任务；失败或死信只允许 CAS 重排。
  - 知识库删除先建立不可取消屏障和任务账本，后续级联文档删除；任务重试要求知识库仍处于 `DELETING`。
- 引用与证据闭环已明确：
  - 最终回答只能使用本次真实模型调用注入的引用白名单；伪造引用优先判为无效。
  - 引用详情实时复核知识库、文档、活动版本、代际、分块、内容哈希、可见性和所有者，生命周期漂移后立即不可用。
  - 内存证据仓以租户、用户、会话、运行四元键隔离，限制容量与 TTL；相同检索 ID 或引用 ID 指向不同证据时拒绝合并。
- 零上下文 diff 审计未发现新增可执行语句；本批修改路径 `git diff --check` 通过。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行 18 个知识库、文档、授权、会话 RAG、引用和证据测试类：共 82 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第八批执行计划（RAG 在线检索主链路）

范围：

- `RagRetrievalService`
- `RagContextContributor`
- `RagRetrievalDebugService`

本批重点：

- 明确运行快照、目标绑定、知识库代际、检索策略和用户查询如何组成一次检索请求。
- 明确查询改写、Dense、Sparse、RRF/加权融合、去重、阈值过滤、重排、邻居扩展、父块扩展、Token 预算和引用封装的执行顺序。
- 明确必需绑定与可选绑定、超时与外部服务错误、允许降级与禁止降级的边界。
- 明确检索证据如何进入 Context Manager、如何绑定模型调用 ID，以及调试接口与真实聊天链路的口径一致性。
- 明确阶段耗时、候选数量和唯一链路标识的可观测语义。

门禁：

- 逐方法、内部记录和关键分支审计，只允许注释变化。
- 领域层干净编译，并运行在线检索、上下文贡献、降级、引用和调试相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第八批操作记录（RAG 在线检索主链路）

- 已逐方法审计 `RagRetrievalService`、`RagContextContributor` 和 `RagRetrievalDebugService`，补齐依赖、阶段算法、范围校验、降级规则、内部记录与诊断采集注释。
- 在线检索执行顺序已明确：
  - 规范问题并按运行创建时固化的 bindingId 快照收窄目标绑定，快照失效时禁止静默改用其他知识库。
  - 实时解析知识库生命周期、当前代际、检索策略和用户可见性；必需绑定不可用立即失败，可选绑定不可用允许跳过。
  - 按策略生成 Dense/Sparse 查询表示，逐绑定执行双路召回、范围一致性检查、RRF/加权融合、阈值过滤、内容哈希去重和可选 Rerank。
  - Rerank 只能返回输入白名单中的唯一 chunkId；失败时按融合排序降级，范围违规不能被可选绑定降级吞掉。
  - 候选回表后复核知识库、文档、活动版本和代际；删除并发窗口只允许已验证的墓碑命中被丢弃。
  - 主命中可扩展同版本父块和相邻块；最终同时受全局、绑定和 `finalTopK` 预算约束，再生成绑定本次检索和分块的引用 ID。
- 上下文与调试口径已明确：
  - `RagContextContributor` 只在可信运行目标与 RAG Token 预算完整时调用生产检索链路，用转义后的无指令权 XML 注入资料。
  - 实际引用同步转为模型调用证据白名单，供最终回答引用校验使用。
  - 调试接口先校验租户管理员和真实目标绑定，再开启有界候选轨迹；轨迹不保存查询正文或候选正文。
- 可观测性已明确：每个阶段记录 `traceId`、`retrievalId`、输入/输出数量、耗时和结果；绑定阶段额外记录 binding、知识库、代际、策略 revision 与 required。
- 零上下文 diff 审计未发现新增可执行语句；3 个文件 `git diff --check` 通过。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行 4 个在线检索、上下文贡献和调试测试类：共 30 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第九批执行计划（资产、认证、存储与用量）

范围：

- `domain/asset` 7 个 Java 文件。
- `domain/auth` 15 个 Java 文件。
- `domain/storage` 6 个 Java 文件。
- `domain/usage` 4 个 Java 文件。
- `domain/business` 5 个占位 `package-info.java`。

本批重点：

- 明确附件上传、归属、解析状态、文本贡献和跨会话引用边界。
- 明确注册、登录、密码修改、资料更新、Access/Refresh Token 和刷新令牌轮换/撤销语义。
- 明确对象存储 bucket/key、流式上传下载、大小和内容哈希契约。
- 明确模型 Token 用量的幂等记录、运行归属、取消处理和聚合统计口径。
- 明确空业务包只提供分层占位，不承载隐式行为。

门禁：

- 37 个文件逐一审计；已有准确注释保留，缺失语义补齐，只允许注释变化。
- 领域层干净编译，并运行资产、认证、对象存储和模型用量相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第九批操作记录（资产、认证、存储与用量）

- 已逐一审计 37 个文件，29 个文件补齐或收敛注释，其余文件原有注释已经准确覆盖契约。
- 资产链路已明确：
  - 上传命令的租户和用户来自可信上下文；可选会话先校验访问权。
  - 原文件按用户和内容哈希复用对象，数据库登记失败时补偿新对象；补偿失败不掩盖原始数据库异常。
  - 附件绑定要求 active、ready、属于当前用户和会话且尚未绑定其他消息；上下文只读取有效消息已绑定附件。
  - 附件上下文按最近消息优先占预算、按时间正序渲染，并按内容哈希去重和 XML 转义。
- 认证链路已明确：
  - 注册同时创建租户与首个 owner，密码只保存哈希。
  - 登录同时校验平台用户、租户成员关系、密码状态和密码过期时间。
  - Refresh JWT 还必须匹配服务端 active 刷新令牌哈希；重新签发会保存新刷新令牌凭证。
  - 修改密码后撤销当前用户刷新令牌，阻止旧长期凭证继续换取访问令牌。
- 存储与用量已明确：
  - 对象存储命令区分堆内小对象与受控暂存文件流式上传，下载必须限定根目录、相对路径和字节上限。
  - 模型用量以调用和 invocation 标识幂等记录 running/success/failed/cancelled；Token 不得为负，总量缺失时由输入与输出相加。
  - 聚合空值统一为零，近期统计限定 1 到 90 天，取消只更新可信运行范围内的 running 调用。
- 5 个业务占位包已明确职责；4 个历史 `package-info.java` 原本无末尾换行，修改注释后 diff 重显了内容完全相同的 `package` 声明，未改变包名或运行行为。
- 本批路径 `git diff --check` 通过；除上述 EOF 换行导致的同值 `package` 重显外，新增内容全部为注释。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行 8 个资产、认证仓储/过滤器、JWT、MinIO 和模型用量测试类：共 32 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第十批执行计划（Agent 端口、模型与可用性）

范围：

- `domain/agent/adapter` 下仓储端口与占位包。
- `domain/agent/model` 下聚合、实体、值对象、枚举与自动配置属性。
- `AgentAvailabilityService`、`IArmoryService`、`IChatService`。

本批重点：

- 明确平台静态 Agent、租户覆盖配置和最终可用性的合并规则。
- 明确 Agent、模型、API、MCP、Skill、Advisor 和 Workflow 关系配置的字段语义。
- 明确聊天命令中的可信用户、会话、运行、消息、附件和运行时 Agent 身份。
- 明确装配端口与聊天流端口的输入、输出、副作用和生命周期。

门禁：

- 端口、模型和可用性服务逐文件审计，只允许注释变化。
- 领域层干净编译，并运行 Agent 可用性、配置映射和聊天授权相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第十批操作记录（Agent 端口、模型与可用性）

- 已审计 Agent 仓储端口、配置模型、租户覆盖实体、聊天/装配命令、类型枚举、自动配置属性及三个服务端口，共 12 个 Java 文件。
- 已明确平台 Agent 的静态启用状态与租户覆盖状态分层：租户未配置时继承平台状态，显式覆盖仅影响当前租户，平台禁用时租户不能反向启用。
- 已明确 Agent 配置关系：
  - API、模型、MCP、Skill、Advisor、Agent、Workflow 与 Runner 配置分别描述注册材料，不代表对应运行时对象已经完成装配。
  - `AiAgentRegisterVO` 是运行入口所需的 Agent 身份快照；类型码必须通过枚举映射，未知类型不能被静默解释。
  - 自动配置属性只承载启动期装配清单和开关，不负责运行期租户授权。
- 已明确聊天命令边界：用户、会话、运行、运行时 Agent、消息与附件 ID 共同限定一次调用；可信租户身份不从前端命令体接收。
- 已明确仓储、装配与聊天流端口的职责和副作用：仓储提供配置与租户覆盖持久化，装配端口构造运行时依赖，聊天端口返回惰性文本流。
- 本批路径 `git diff --check` 通过；零上下文 diff 审计未发现新增可执行语句。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行 Agent 可用性与聊天授权测试：共 3 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。
- 额外运行历史集成测试 `AiAgentAutoConfigTest` 时，其 3 个用例均因本地缺少名为 `100001`、`100002` 的动态 Agent Bean，以及工具测试未提供完整运行身份而报错；错误发生在读取外部装配结果和执行工具解析时，与本批纯注释变化无关，未修改业务代码掩盖该环境前置条件。

### 2026-07-26：领域层第十一批执行计划（Agent 聊天运行主链路）

范围：

- `domain/agent/service/ChatService.java`。

本批重点：

- 明确普通 Agent、工作流 Agent、续跑、引导和附件消息的入口差异。
- 明确用户消息持久化、Context Manager 组装、RAG/附件上下文贡献、模型调用和助手消息落库的先后顺序。
- 明确运行身份如何进入工具解析，模型事件如何转换为文本流，以及 Token 用量与证据如何绑定模型调用。
- 明确调用工具前取消门禁、流式取消、失败收口、证据清理和运行终态推进。
- 明确工作流 DAG 的运行时加载、节点调度、事件流转、暂停/恢复和最终输出路径。

门禁：

- 对 1204 行聊天服务逐方法、内部记录和关键分支审计，只允许注释变化。
- 运行零上下文 diff 与格式检查、领域层干净编译，并执行聊天授权、SSE、运行控制、上下文与工作流相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第十一批操作记录（Agent 聊天运行主链路）

- 已逐方法审计 `ChatService` 1204 行源码，补齐依赖职责、入口差异、运行状态、模型调用、DAG 调度、取消门禁、证据收口和终态持久化注释。
- 普通 Agent 链路已明确：
  - 先校验公共 Agent 与租户可用性，再创建/校验业务会话和运行。
  - 用户消息与附件原子绑定运行后才调用模型；`message` 在 `runner.runAsync(...)` 处作为 ADK `Content` 交给 Agent。
  - ADK 状态只注入服务端可信的租户、用户、会话、运行、上下文版本和 RAG 快照；模型文本不能伪造工具身份。
  - 流式事件逐个通过运行门禁，正常完成、异常和取消竞争时最多写一次助手终态。
- 工作流链路已明确：
  - 入口先冻结实际版本、模型、DAG 与内部 Agent，订阅文本流后才在协调线程执行。
  - Kahn 拓扑算法按层推进，同层节点并行、下层等待全部直接依赖；自循环不计入拓扑入度，只触发最多 20 次受控迭代。
  - 用户输入、上游输出和上一轮输出被确定性组装成节点 prompt；节点 prompt 在各自 `runner.runAsync(...)` 处进入内部 Agent。
  - 最终回复只拼接终点节点输出，RAG 证据只保留终点祖先链并在消息落库前校验引用。
- 取消边界已明确：跨实例取消由数据库状态轮询接收，客户端断流反向写取消；协调任务、节点循环、模型事件与工具调用前均依赖可执行门禁，取消后清理临时 RAG 证据。
- 使用“剥离注释后源码对比”确认新增差异只有注释及其占位空白；`git diff --check` 通过。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行聊天授权、SSE、运行控制、执行门禁、上下文组装/压缩、工作流编译/运行时/线程池相关 10 个测试类：共 45 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第十二批执行计划（Agent 装配骨架与节点链）

范围：

- `domain/agent/service/armory` 根目录与 `factory`。
- `domain/agent/service/armory/node` 及其 `workflow` 子目录。

本批重点：

- 明确责任链装配顺序、节点间传递的数据、缺失配置的失败边界和重复装配语义。
- 明确 API 配置如何生成模型客户端，模型如何绑定工具/Skill/插件并构造原子 Agent。
- 明确顺序、并行、循环工作流 Agent 如何引用子 Agent，以及 Runner 如何成为最终可调用运行体。
- 明确公共静态 Agent 与工作流动态 Agent 的注册键、生命周期和并发可见性。

门禁：

- 12 个文件逐方法和关键分支审计，只允许注释变化。
- 运行剥离注释源码对比、格式检查、领域层干净编译，以及自动配置、工作流编译和 Agent 注册相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第十二批操作记录（Agent 装配骨架与节点链）

- 已逐一审计装配基类、装配服务、默认工厂、6 个主节点和 3 个组合 Agent 节点，共 12 个 Java 文件。
- 装配顺序已明确：`RootNode -> AiApiNode -> ChatModelNode -> AgentNode -> AgentWorkflowNode（循环消费配置）-> RunnerNode`。
- 配置到运行体的转换已明确：
  - API 节点构造 OpenAI 兼容客户端，并只对聊天补全请求尝试关闭供应商思考字段。
  - 模型节点构造默认 ChatModel，清空旧式 ToolCallback；工具只能经 `GatewayToolset` 进入运行门禁。
  - 原子 Agent 可复用默认模型或只覆盖模型代码，按配置名称放入当前装配上下文。
  - 组合 Agent 按配置类型构造 Loop、Parallel 或 Sequential Agent，并回写同一名称索引供嵌套引用。
  - Runner 按配置名称选择根 Agent，解析显式插件并强制补齐上下文注入与工具执行守卫，最后按 agentId 注册 `AiAgentRegisterVO`。
- 生命周期已明确：每张配置表使用独立 `DynamicContext`；责任链节点为无状态 Spring Bean，最终运行体是可被同名重装配替换的单例。
- 已指出现有 `queryAgentList` 对缺失子 Agent 名称直接跳过的实际行为，注释未把它误写成强校验。
- 对本批 12 个文件执行剥离注释并删除空行后的源码对比，未发现任何可执行代码变化；`git diff --check` 通过。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行工作流编译、运行时解析、生命周期、线程池配置、Agent 可用性和聊天授权 6 个测试类：共 19 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第十三批执行计划（Agent MCP、Skill 与运行插件）

范围：

- `domain/agent/service/armory/matter` 下 model、mcp、plugin、skills 全部 14 个 Java 文件。

本批重点：

- 明确本地、SSE、Stdio MCP 客户端的创建条件、连接生命周期和工具暴露边界。
- 明确 Skill 文件发现、解析、资源读取和 ToolCallback 构造规则。
- 明确上下文插件在模型调用前如何注入数据库历史、附件与 RAG，以及证据如何绑定 invocation。
- 明确工具守卫如何在工具真正执行前校验运行取消/版本状态。
- 明确模型观测如何采集请求、响应、Token、耗时和异常，日志插件如何避免泄漏敏感正文。

门禁：

- 14 个文件逐方法和关键分支审计，只允许注释变化。
- 运行剥离注释源码对比、格式检查、领域层干净编译，以及上下文、工具、用量和链路观测相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第十三批操作记录（Agent MCP、Skill 与运行插件）

- 已逐一审计 model 2 个、MCP 6 个、plugin 4 个、skills 2 个，共 14 个 Java 文件。
- MCP 边界已明确：
  - local 按受控 Bean 名称读取本地工具提供器；SSE 在装配期解析端点并同步初始化远端客户端；Stdio 只从配置读取命令、参数和环境变量并启动子进程。
  - MCP 初始化失败会阻止 Agent 发布，成功后的 ToolCallback 复用对应客户端连接。
  - 工厂的实际冲突优先级为 local、SSE、Stdio；注释如实记录现有行为，没有新增互斥校验。
- Skill 边界已明确：
  - 文件系统目录直接交给 `SkillsTool`；Jar 内 classpath 目录释放到隔离临时目录。
  - 资源复制执行规范化和根目录边界校验，空目录拒绝装配。
- 运行插件已明确：
  - 上下文插件只从可信 state 组装数据库历史、压缩摘要、附件、上游输出和 RAG；必需 RAG 不可用或范围违规时失败关闭。
  - 工具守卫在真实工具执行前检查取消与上下文版本；调用前压缩发生时短路本次工具，要求模型基于新版本重新推理。
  - 模型适配器保留供应商模型名和 Token 元数据；日志插件只记录身份、阶段、耗时和用量，不主动记录提示词或回答正文。
  - ThreadLocal 观测快照在回调结束清理；流式累计 Token 通过五字段指纹去重。
- 删除了 `SSEToolMcpCreateService` 中带真实 API Key 形态的历史示例 URL 注释；未改连接配置或业务逻辑。
- 对 14 个文件执行剥离注释后的源码对比；12 个文件完全一致，另外 2 个历史文件仅补齐末尾换行，所有可执行语句一致；`git diff --check` 通过。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行模型观测、上下文、用量、运行门禁和 MCP 协议/传输支持 7 个测试类：共 24 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第十四批执行计划（工具端口与领域模型）

范围：

- `domain/tool/adapter`、`domain/tool/model` 下 22 个 Java 文件。

本批重点：

- 明确工具仓储端口覆盖的目录、版本、发布、调度租约与调用审计能力。
- 明确 MCP/Skill 创建命令、版本实体、连接配置和包上传结果的字段语义。
- 明确工具目录中的类型、可见性、状态、租户归属与版本引用边界。
- 明确调用上下文、用户上下文、调度领取和调用统计的可信来源与幂等键。
- 明确 ADK state 中所有工具运行键的生产者和消费者契约。

门禁：

- 22 个文件逐字段、方法和状态转换审计，只允许注释变化。
- 运行剥离注释源码对比、格式检查、领域层干净编译，以及工具仓储、发布、授权和状态相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第十四批操作记录（工具端口与领域模型）

- 已逐一审计工具仓储端口与领域模型 22 个 Java 文件；15 个文件补齐字段/常量语义，7 个文件原有注释已准确覆盖契约。
- 工具目录与版本关系已明确：
  - Skill/MCP 定义保存稳定业务身份、所有者、可见性和当前/发布/激活版本指针。
  - 版本实体冻结包资产或连接配置；运行目录只暴露当前已发布版本所需字段。
  - 数据库主键不作为外部标识，版本号与版本记录 ID 的用途不混用。
- 调用审计已明确：
  - `ToolInvokeContextEntity` 只承载 ChatService 注入的可信身份、运行、上下文版本和函数调用 ID。
  - `ToolCallLogEntity.idempotencyKey` 是领取外部副作用执行权的唯一键；调用日志区分 started/success/failed。
  - 分享依赖只从会话内有效成功调用聚合，统计区分调用总数与去重工具数。
- ADK state 常量已逐项标明生产/消费语义，包含历史/附件切面、RAG 快照和工作流节点证据 invocation。
- Skill 包配置已明确最大条目数与单条目字节上限，作为 Zip Bomb 防线。
- 对 22 个文件执行剥离注释后的源码对比，所有可执行语句一致；`git diff --check` 通过。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行工具发布、调度授权、Skill 包读取和 MCP 协议/传输 5 个测试类：共 13 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第十五批执行计划（工具发布与执行主链）

范围：

- `domain/tool/service` 及其 `mcp`、`support` 子目录共 13 个 Java 文件。

本批重点：

- 明确 Skill/MCP 创建、测试、发布、停用和版本切换的权限与状态门禁。
- 明确工具解析如何按租户、用户、类型、版本和可见性得到唯一运行目标。
- 明确 GatewayToolset/ADK Tool 如何传递可信运行上下文，ToolGateway 如何先授权与幂等领取再产生副作用。
- 明确 Skill 包读取的目录边界、manifest 校验和资源限制。
- 明确 SSE/Stdio MCP 连接、协议初始化、工具 schema 发现、调用和关闭责任。

门禁：

- 13 个文件逐方法和关键分支审计，只允许注释变化。
- 运行剥离注释源码对比、格式检查、领域层干净编译，以及工具发布、授权、网关、Skill 包和 MCP 传输相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第十五批操作记录（工具发布与执行主链）

- 已逐一审计工具服务 13 个 Java 文件；8 个文件补强注释，5 个文件原有契约已准确覆盖实现。
- 发布链路已明确：
  - Skill 上传只登记受限 ZIP 资产；创建定义时重新读取并验证 `SKILL.md`，草稿版本不会改变已发布版本。
  - Skill/MCP 发布原子推进定义与版本状态；停用只使定义退出运行目录，保留历史版本和调用审计。
  - MCP 当前版本必须完成连接测试并发现 schema 后才能发布；测试失败作为领域结果持久化。
  - 能启动本机进程的 Stdio/local MCP 只允许租户 owner/admin 配置。
- 执行链路已明确：
  - `GatewayToolset` 每轮按可信身份动态查询目录，发布、停用和权限变化无需重装配 Agent。
  - `GatewayAdkTool` 只包装模型 schema，不从模型参数构造身份。
  - `ToolDispatchAuthorizationService` 在短事务中先锁定运行，再用唯一索引领取幂等执行权。
  - `ToolGateway` 只有在 `claimed=true` 后才调用 Skill/MCP；重复请求只重放 success/failed，started 未知态也不二次执行。
  - Skill 只返回已发布包的指令文本，不执行包内代码；SSE/Stdio 每次协议操作均在关闭作用域内 initialize/listTools/callTool。
- Skill ZIP 已明确总包、条目数、单条展开字节和严格 UTF-8 边界；远程工具结果统一截断后再进入模型。
- 对 13 个文件执行剥离注释后的源码对比，所有可执行语句一致；`git diff --check` 通过。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行工具发布、授权、幂等网关、Skill 包读取和 MCP 协议/传输 6 个测试类：共 15 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层第十六批执行计划（工作流定义、编译与运行时）

范围：

- `domain/workflow` 全部 18 个 Java 文件。

本批重点：

- 明确工作流定义、草稿图、不可变版本、当前发布指针和运行时快照的关系。
- 明确节点、边、入口、终点、自循环、并行层和最大迭代的 DAG 编译规则。
- 明确节点选择的 Agent、模型、Skill、MCP 与选项如何经授权解析成运行时配置。
- 明确创建、保存草稿、发布、启停、查询和版本回滚的权限与并发边界。
- 明确模型路由覆盖顺序及动态运行时 Agent 的生命周期。

门禁：

- 18 个文件逐字段、方法和关键分支审计，只允许注释变化。
- 运行剥离注释源码对比、格式检查、领域层干净编译，以及工作流编译、运行时、生命周期和授权相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：领域层第十六批操作记录（工作流定义、编译与运行时）

- 已逐一审计 `domain/workflow` 全部 18 个 Java 文件；6 个文件补强关键执行语义，其余 12 个文件原有注释已覆盖字段和端口契约。
- 生命周期已明确：
  - 创建同时生成版本 1 草稿；保存草稿只推进当前草稿版本，不改变已发布版本。
  - 发布阶段校验图并推进发布指针；动态 Agent 在首次 `loadRuntime` 时懒编译、装配和缓存。
  - 运行时只能加载 `published` 版本；请求模型覆盖、版本和工作流共同构成缓存隔离键。
  - 草稿保存、发布和删除会清理对应运行时缓存；删除前必须确认不存在未完成运行。
- DAG 编译已明确：
  - 当前运行计划只编译 LLM 节点，非 LLM 画布节点及其边不会进入执行 DAG。
  - 显式根节点优先；缺省时选择首个零入度节点，并补齐不可达节点以保持编译结果稳定。
  - Kahn 拓扑校验拒绝普通环路；自循环由节点 `maxIterations` 限制，不计入拓扑入度。
  - 每个节点独立装配 Runner，节点模型覆盖和调用证据互不串用；系统 AI API 通道只复制配置，不在工作流定义中保存密钥。
- 模型路由已明确按“请求覆盖、节点覆盖、工作流默认、平台默认”取首个非空值，并对结果执行严格白名单校验，未知模型不静默回退。
- 对 18 个文件执行剥离注释后的源码对比，所有可执行语句一致；`git diff --check` 通过。
- 执行领域层干净编译，281 个源文件编译成功。
- 使用 Java 25 兼容参数运行工作流 DAG 编译、运行时编译、生命周期、执行器配置、聊天授权和 RAG 绑定目标授权 6 个测试类：共 21 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：领域层整体闭环审计计划

范围：

- `ai-agent-scaffold-domain/src/main/java` 全部 281 个 Java 文件。

门禁：

- 以注释改造开始前的 `9531e8c` 为代码基线，逐文件剥离块注释、行注释和空行后比对可执行源码。
- 核对领域层文件总数与分批覆盖清单，确认没有漏包、漏文件或跨层误改。
- 再执行一次领域层干净编译；若任何可执行源码不同或编译失败，先定位并修正，不能宣告领域层完成。
- 将真实审计结果追加在本节后，并用中文本地提交形成领域层闭环。

### 2026-07-26：领域层整体闭环审计记录

- 清点领域层主源码共 281 个 Java 文件，与基线 `9531e8c` 的文件集合一致，无新增、删除或漏审文件。
- 首轮审计发现 `domain/agent/service/package-info.java` 没有中文包语义，已补充“Agent 对话与运行时装配的领域服务”。
- 使用统一末尾换行后的注释剥离结果逐文件比对：281 个文件的可执行源码差异为 0。
- 检查每个文件是否至少包含一处中文语义说明：缺失数为 0。
- `git diff --check` 通过；领域层干净编译再次编译 281 个源文件，`BUILD SUCCESS`。
- 领域层注释改造完成，后续只在最终全项目门禁中再次验证，不再扩展业务逻辑。

### 2026-07-26：基建层第一批执行计划（DAO 接口）

范围：

- `infrastructure/dao` 目录下 36 个 MyBatis DAO 接口和 1 个包说明文件，共 37 个 Java 文件。

本批重点：

- 明确普通查询、悲观锁查询、条件更新、幂等插入和软删除的数据库语义。
- 明确调度任务领取、运行取消、上下文压缩、RAG outbox/租约和工具调用日志中的并发门禁。
- 明确租户、用户、会话、版本和状态条件必须下推到 SQL，不能由调用方事后过滤。
- 对已有准确注释不机械改写；只补方法结果、影响行数和并发冲突含义。

门禁：

- 37 个文件逐接口、方法和参数审计，只允许注释变化。
- 执行剥离注释源码比对、格式检查、基建层干净编译和 DAO 映射装载相关测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：基建层第一批操作记录（DAO 接口）

- 已逐一审计 36 个 DAO 接口和 1 个包说明文件；19 个接口补强方法级注释，其余 18 个文件原有契约已覆盖实现。
- 并发写入语义已明确：
  - 调度配置协调使用配置哈希、版本和旧更新时间 CAS；任务领取、续租和完成必须校验租约所有者与 fencing token。
  - 分享访问次数在悲观锁内消费；导入记录以接收范围唯一键阻止重复导入。
  - 压缩任务按任务键幂等插入，取消后将覆盖失效序列的任务和记忆快照标旧，并只恢复经验证安全的历史快照。
  - RAG 摄取与级联删除区分扫描候选、领取租约、心跳续租和带围栏/修订号推进，返回 0 明确表示冲突或失权。
- 租户、所有者、会话、知识库、版本和状态条件均在 DAO 契约中明确为 SQL 边界；调用方不能依赖事后过滤弥补越权查询。
- 对 37 个文件执行剥离注释后的源码比对，可执行源码差异为 0；方法声明与方法注释覆盖检查缺口为 0；`git diff --check` 通过。
- 基建层干净编译成功，130 个源文件编译通过。
- 运行 MyBatis 全量映射装载及 RAG 摄取、知识库、outbox、会话 RAG 策略 5 个测试类：共 8 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：基建层第二批执行计划（核心持久化对象）

范围：

- `infrastructure/dao/po` 中调度、工作流、资产、会话、运行、上下文、MCP 和模型用量相关的前 19 个 PO 文件。

本批重点：

- 明确数据库主键与业务 ID、租户与所有者、可见性与状态字段不可混用。
- 明确调度配置/任务/执行账本中的 Cron 游标、配置哈希、租约、围栏令牌和重试字段。
- 明确会话、消息、运行、分享和导入快照中的序列、有效性、取消、访问上限和幂等字段。
- 明确上下文压缩任务、记忆快照、MCP 版本和模型 Token 用量的状态与版本关系。
- `BasePO` 的审计时间字段只描述数据库审计信息，不承担领域版本控制。

门禁：

- 19 个文件逐字段审计，所有持久化字段都必须有一针见血的中文语义。
- 执行字段覆盖检查、剥离注释源码比对、格式检查、基建层干净编译和相关 PO/仓储测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：基建层第二批操作记录（核心持久化对象）

- 已逐一审计调度、工作流、资产、会话、上下文、MCP 和模型用量 19 个 PO；12 个文件补强字段语义，7 个文件原有注释已覆盖全部字段。
- 调度三表关系已明确：
  - 配置保存用户声明、执行身份和协调版本；任务保存从配置协调出的唯一可领取运行时及 Cron 游标；执行表按触发键保存单次计划发生的幂等终态。
  - 租约所有者决定当前执行权，fencing token 隔离过期执行者，row/config version 分别承担任务和配置并发控制。
- 会话状态已明确：
  - `ChatRunPO` 冻结运行目标、RAG 策略和上下文基线，并以 predecessor/successor 表达 steer 接替链。
  - 分享只保存下载令牌摘要、对象位置、完整性摘要和访问计数；导入以接收范围键去重。
  - 压缩任务保存消息覆盖区间和租约状态；记忆快照保存父版本、覆盖摘要和 active/superseded/stale 状态。
- `BasePO.updateTime` 明确仅是数据库审计时间，不替代领域修订号；模型用量区分调用状态、停止原因和各类 Token。
- 19 个文件剥离注释后与批次基线逐一一致，可执行源码差异为 0；除原本按三组同行声明的压缩任务 PO 外，其余文件类与字段注释计数完整，压缩任务三组字段也已逐组说明；`git diff --check` 通过。
- 基建层干净编译成功，130 个源文件编译通过。
- 运行调度协调/派发、Cron、会话分享、模型用量、记忆 PO 与记忆服务 7 个测试类：共 28 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：基建层第三批执行计划（RAG 持久化对象）

范围：

- `infrastructure/dao/po` 中以 `Rag` 开头的 14 个 PO 文件。

本批重点：

- 明确知识库、文档、文档版本、分块之间的稳定身份、generation 和活动版本指针。
- 明确摄取、outbox、知识库删除任务中的幂等键、阶段、checkpoint、尝试、租约、围栏令牌和修订号。
- 明确候选扫描对象只携带领取所需最小身份，不作为完整任务快照。
- 明确检索策略、检索记录和引用记录分别保存配置、一次运行摘要与最终证据，不能相互替代。
- 明确向量点 ID、内容摘要、解析资产和元数据字段的生产者与核验用途。

门禁：

- 14 个文件逐字段审计，只允许注释变化。
- 执行字段覆盖检查、剥离注释源码比对、格式检查、基建层干净编译和 RAG 持久化/仓储测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：基建层第三批操作记录（RAG 持久化对象）

- 已逐一审计 14 个 RAG PO 文件；全部文件的类、记录组件或持久化字段均有明确中文语义。
- 索引身份已明确：
  - 逻辑文档 ID 跨版本稳定，versionId 标识不可变版本，generation 隔离重建代次，activeVersionId/activeGeneration 只指向已完整就绪索引。
  - chunkId 是业务分块身份，vectorPointId 是 Qdrant 点身份；内容摘要用于幂等与证据核验，结构路径、页码和字符偏移用于可解释引用。
  - 知识库只保存稳定 collection alias，物理集合切换不泄漏到调用方。
- 异步账本已明确：
  - 摄取、outbox 和知识库删除任务都区分幂等键、尝试、下次重试、租约、心跳、fencing token 与 row version。
  - checkpoint 保存可恢复阶段；候选投影只携带 tenantId 与任务/事件 ID，领取后必须重新读取受保护状态。
  - outbox 的 partitionKey 保证同聚合有序，publishedAt 只在 Broker 确认后写入。
- 检索审计已明确：
  - Profile 保存算法开关、TopK、权重、阈值和 Token 预算。
  - Record 冻结策略修订、候选数量、各阶段耗时与降级/错误摘要。
  - Citation 保存最终返回证据的来源版本、分块、向量点、各路得分与正文完整性摘要。
- 14 个文件字段覆盖检查缺口为 0；剥离注释后可执行源码差异为 0；`git diff --check` 通过。
- 基建层干净编译成功，130 个源文件编译通过。
- 运行 RAG codec、映射、仓储、检索审计、文档删除登记、知识库删除和冲突映射 7 个测试类：共 39 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：基建层第四批执行计划（工具、身份与会话 RAG PO）

范围：

- `infrastructure/dao/po` 剩余 10 个文件：会话 RAG 绑定选择、Skill 定义/版本、租户/租户用户、工具调用日志/统计、用户账号/密钥和包说明。

本批重点：

- 明确会话 RAG 选择是策略快照，绑定 ID 列表与 policy revision 共同决定运行时一致性。
- 明确 Skill 定义的版本指针与不可变版本资产、内容摘要、manifest 和状态关系。
- 明确租户成员角色/状态、账号身份与加密密钥引用的边界，敏感值不进入注释。
- 明确工具调用日志的幂等键、可信身份、运行上下文、终态、耗时和审计字段。

门禁：

- 10 个文件逐字段审计，只允许注释变化。
- 完成全部 43 个 PO 的类/字段中文语义覆盖检查、剥离注释源码比对、格式检查、编译和工具/身份/RAG 策略测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：基建层第四批操作记录（工具、身份与会话 RAG PO）

- 已逐一审计剩余 10 个 PO 文件；8 个文件补强类或字段语义，工具调用日志和 Skill 版本原有注释已完整覆盖。
- 会话 RAG 手动选择已明确必须同时限定租户、用户、会话和实际运行目标，selectionOrder 决定多绑定顺序；运行时仍以会话 policy revision 冻结一致快照。
- Skill 定义明确为稳定身份与版本指针，SkillVersion 保存不可变资产、摘要和 manifest；二者不能合并成可覆盖记录。
- 租户成员角色与账号身份分离；用户密钥 PO 只保存摘要/密文引用、盐和生命周期，不在源码或注释中写入任何真实敏感值。
- 工具调用日志保存可信租户/用户/会话/运行上下文、外部副作用幂等键、输入输出审计、终态和耗时；统计 PO 区分调用总数与去重工具数。
- 对 `dao/po` 全部 43 个 Java 文件完成总审计：普通类均达到类注释加字段注释覆盖，record 组件和压缩任务同行字段具有对应整体契约；剥离注释后可执行源码差异为 0；`git diff --check` 通过。
- 基建层干净编译成功，130 个源文件编译通过。
- 指定测试集合中实际发现并运行会话 RAG 策略、工具调度授权和认证仓储 3 个测试类：共 6 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`；未发现的测试类未计入结果。

### 2026-07-26：基建层第五批执行计划（仓储适配器）

范围：

- `infrastructure/adapter` 全部 22 个 Java 文件。

本批重点：

- 明确领域实体与 PO 的转换边界，数据库异常如何映射为领域冲突、未找到或幂等重放。
- 明确事务边界：RAG 上传/删除登记、会话分享、运行状态、调度领取和工具调用领取必须原子闭合。
- 明确所有读写都使用可信租户/用户条件，缓存仅作优化，不能成为授权事实源。
- 明确 JSON 编解码、空值兼容、状态映射和影响行数检查的失败语义。

门禁：

- 22 个文件逐方法、事务和关键映射分支审计，只允许注释变化。
- 执行剥离注释源码比对、格式检查、基建层干净编译和仓储单元测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：基建层第五批操作记录（仓储适配器）

- 已逐一审计 `infrastructure/adapter` 全部 22 个 Java 文件；8 个高风险或原注释稀疏的仓储补强关键方法与事务语义，其余 14 个文件原有端口契约与映射说明已覆盖实现。
- 薄适配与领域仓储边界已明确：
  - `PlatformRepository` 仅作基础 DAO 一对一透传，不在其中隐式补授权或事务。
  - 授权事实始终来自带可信租户/用户条件的数据库查询；缓存和 JSON 快照只用于性能或还原，不能成为授权依据。
- 事务闭环已明确：
  - RAG 上传登记原子写任务、文档、版本和 outbox；文档删除登记先锁聚合并拒绝活动摄取冲突。
  - RAG Worker 更新同时校验租户、任务、leaseOwner、fencingToken 和 revision；成功激活、删除、取消与失败均在事务内收口任务及索引墓碑。
  - 知识库级联删除只有在文档、版本、分块和摄取任务全部关闭后才能完成。
  - 调度执行终态与 Cron 游标/重试推进必须同事务；会话分享在锁内消费访问次数，导入按接收范围幂等。
- `ChatRunRepository` 明确运行状态 CAS、steer 前后继关系、上下文修订推进和 RAG 绑定快照 JSON 的失败语义。
- 对 22 个文件执行剥离注释源码比对，可执行源码差异为 0；每个文件均包含中文职责或关键分支说明；`git diff --check` 通过。
- 基建层干净编译成功，130 个源文件编译通过。
- 指定集合中实际运行 RAG 仓储/删除登记/知识库删除、调度协调/派发、工作流生命周期、分享、认证和记忆服务 9 个测试类：共 55 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：基建层第六批执行计划（RAG 客户端、持久化辅助与 Worker）

范围：

- `infrastructure/rag` 全部 20 个 Java 文件。

本批重点：

- 明确 Docling/本地解析、TEI Embedding/Rerank 和 Qdrant 请求协议、超时、响应校验与瞬态/永久错误分类。
- 明确 Word、Markdown、Docling JSON 到结构化 IR 的保真边界与降级路径。
- 明确 outbox 领取发布、Worker 调度、摄取阶段、checkpoint、心跳、取消屏障和 generation 隔离。
- 明确知识库级联删除的步骤顺序、幂等清理和失败恢复。
- 明确持久化 codec/mapper 只做稳定格式转换，检索审计不允许反向影响在线结果。

门禁：

- 20 个文件逐方法、外部调用和关键失败分支审计，只允许注释变化。
- 执行剥离注释源码比对、格式检查、基建层干净编译和 RAG 协议/Worker/持久化测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：基建层第六批操作记录（RAG 客户端、持久化辅助与 Worker）

- 已逐一审计 `infrastructure/rag` 全部 20 个 Java 文件；15 个文件补强外部协议、阶段和失败语义，5 个配置/codec/工作区文件原有注释已覆盖实现。
- 外部协议已明确：
  - TEI Embedding/Rerank 在单次总 deadline 内分批调用，只重试连接、超时和瞬态 HTTP 状态；响应体、文本长度、索引、向量数量/维度及 NaN/Infinity 均有硬门禁。
  - Qdrant 首次使用严格校验 named Dense/Sparse schema；所有查询与删除都在服务端带 tenantId、知识库 generation 或 versionId 过滤，返回 payload 只接受可信标量字段。
  - Docling 调用使用受控文件、有界 multipart、有限重试和严格响应解析；Markdown/DOCX 本地解析保留 AST/OOXML 的标题、列表、代码、表格、页眉页脚和 provenance。
- Worker 副作用已明确：
  - Kafka 与数据库扫描都只负责提交候选；必须先领取数据库租约才能下载、解析、写向量或删除对象。
  - 每个外部副作用前重读取消状态、leaseOwner 与 fencing token；checkpoint 支持安全续跑，generation 隔离旧 Worker。
  - 激活前核对数据库分块与向量点快照；取消/失败只清理目标 generation，不触碰既有活动版本。
  - Canonical IR、normalized Markdown、原始解析输出、质量报告和分块 manifest 全部留痕。
- outbox 明确 Broker ACK 后才写 `published`；失败按尝试次数进入 retry/dead，过期围栏不能覆盖新持有者状态。
- PersistenceMapper 对 0/1 布尔、枚举、租约和 checkpoint 严格转换；检索审计事务失败不会反向改变已产生的在线检索结果。
- 对 20 个文件执行剥离注释源码比对，可执行源码差异为 0；每个文件均包含中文职责或关键失败说明；`git diff --check` 通过。
- 基建层干净编译成功，130 个源文件编译通过。
- 运行 Docling 协议、预处理链路、TEI 协议、Qdrant、outbox、持久化映射/codec、检索审计、摄取 Worker 和知识库删除 10 个测试类：共 84 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：基建层第七批执行计划（附件、上下文与对象存储）

范围：

- `infrastructure/asset`、`context`、`storage` 以及 `gateway`/`redis` 包说明，共 8 个 Java 文件。

本批重点：

- 明确附件文本提取的格式白名单、字节/字符上限、严格编码和解析失败降级。
- 明确上下文压缩事件的 Kafka 唤醒语义，以及模型摘要输入、输出和 Token 预算边界。
- 明确 MinIO 上传、下载、删除、预签名 URL、受控临时文件和对象键规范；对象存储不承担租户授权。
- 明确配置对象中的 endpoint、bucket、超时和凭证只承载运行配置，注释不写真实敏感值。

门禁：

- 8 个文件逐方法和安全边界审计，只允许注释变化。
- 执行剥离注释源码比对、格式检查、基建层干净编译和附件/上下文/对象存储测试。
- 追加真实操作记录后中文本地提交。

### 2026-07-26：基建层第七批操作记录（附件、上下文与对象存储）

- 已审计并补强附件提取、上下文压缩发布/模型调用、对象存储及包边界共 8 个 Java 文件。
- 附件提取明确 MIME 与扩展名白名单、PDF 页数和可注入字符上限；未知二进制与图片只保存资产，解析失败不阻断整轮对话。
- 上下文压缩明确 Kafka 消息只负责唤醒，同租户同会话使用稳定分区键；数据库压缩任务表仍是状态、幂等和补偿事实源。模型端口只执行单轮摘要调用，任务范围、取消屏障和状态推进仍由领域层控制。
- 对象存储明确本类只校验位置、大小和文件系统安全，不承担租户授权；本地上传/下载使用临时文件和原子发布，受控下载拒绝绝对路径、目录穿越及符号链接。
- MinIO 客户端延迟单例化，首次建桶串行化；明确不存在响应才折叠为 `false`，其余服务端错误必须暴露。配置对象不保存真实凭证，密钥由外部受控配置注入。
- 8 个文件剥离注释后与提交前源码完全一致，`COMMENT_ONLY_AUDIT_OK 8/8`；限定基建 Java 路径的 `git diff --check` 通过。
- 基建层干净编译成功，130 个源文件编译通过。
- 实际运行附件提取、附件服务、附件上下文贡献、压缩消费者 Trace 和 MinIO 对象存储 5 个测试类：共 22 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。

### 2026-07-26：基建层第八批执行计划（MyBatis SQL 契约）

范围：

- `infrastructure/src/main/resources/mybatis/mapper` 全部 36 个 XML。

本批重点：

- 每个 Mapper 先说明聚合、租户边界和读写契约；普通 CRUD 不逐字复述 SQL。
- 对行锁、乐观锁 CAS、租约、围栏令牌、幂等插入、冲突更新、软删除和全局扫描单独说明正确性条件。
- RAG 摄取、知识库删除、outbox 和检索审计明确状态推进、旧 Worker 隔离及在线结果不受审计写入影响。
- 会话运行、分享、上下文压缩、调度执行和工具调用明确唯一键、锁顺序与终态写入条件。

门禁：

- 36 个 XML 全部包含中文职责注释，高风险 SQL 具备就近不变量说明。
- 对提交前后 XML 剥离注释和空白后逐文件比对，SQL 与映射内容差异必须为 0。
- 执行 XML 解析、MyBatis Mapper 加载/契约测试和基建层编译；追加真实结果后中文本地提交。

### 2026-07-26：基建层第八批操作记录（MyBatis SQL 契约）

- 已为全部 36 个 Mapper XML 增加聚合级中文契约，说明租户/用户隔离、稳定身份、版本快照、软删除和审计边界。
- 对 9 个高风险 Mapper 增加就近 SQL 不变量说明：
  - 调度配置协调使用配置版本避免旧扫描覆盖新 Cron；运行任务以唯一键冲突更新，领取和完成受租约及围栏保护。
  - 会话运行状态迁移使用 version CAS；取消运行批量失效消息，有效上下文查询排除失效消息。
  - 压缩任务以覆盖范围 taskKey 幂等，领取递增围栏；取消范围重叠任务置 stale，禁止摘要污染。
  - RAG 摄取、知识库删除和 outbox 的全局候选扫描只返回领取身份；真正副作用前按租户领取，终态/checkpoint 必须命中租约、围栏和 revision。
  - 工具调用以幂等键领取副作用，只允许 processing 记录首次写入终态。
- 全部 XML 通过 `xmllint` 解析；每个文件均含中文职责注释。
- 对 36 个文件分别剥离 XML 注释和空白后与提交前版本比对，映射和 SQL 内容差异为 0：`XML_COMMENT_ONLY_AUDIT_OK 36/36`；限定 Mapper 路径的 `git diff --check` 通过。
- 基建层编译成功，36 个 Mapper 资源正常复制。
- 实际运行 MyBatis 全量加载、RAG Mapper/端口契约、调度协调/派发、工作流生命周期、会话分享和工具授权 13 个测试类：共 32 个测试，失败 0、错误 0、跳过 0，`BUILD SUCCESS`。
